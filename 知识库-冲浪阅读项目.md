# 冲浪阅读 Android APP — 项目知识库

> 整理时间：2026-08-15 ｜ 适用版本：v0.4.3（阶段性稳定存档版）
> 定位：原生安卓小说阅读客户端，只调主站（xiaoswz.vercel.app）公开只读 API，不做登录。

---

## 1. 项目概况

| 项 | 内容 |
|---|---|
| 项目名称 | 冲浪阅读（Surf Reader） |
| 包名 | `com.xiaoswz.reader` |
| 仓库 | `lkj314/xiaoswz-reader`（GitHub，已公开，含 Release 资产） |
| 工作目录 | `U:\xiaoswz-reader` |
| 定位 | 主站（xiaoswz.vercel.app）的第三方只读阅读客户端 |
| 当前版本 | v0.4.3（阶段性稳定存档版：封面显示修复 + 书架崩溃修复，2026-08-15） |
| 主站仓库 | `lkj314/novel-site`（私有） |

**特色约束**：APP 不登录、不直连数据库，所有数据来自主站公开只读 API（XiaoswzApi）。

---

## 2. 技术栈

- **语言/UI**：Kotlin + Jetpack Compose (Material 3) + Compose BOM 2024.12.01
- **网络**：Retrofit + OkHttp + kotlinx.serialization
- **图片**：Coil（`material-icons-extended` 已含）
- **本地持久化**：
  - Room 2.6.1（本地书架，**仅书架/进度**，用 KAPT）
  - DataStore（阅读器设置：主题/字号/行距/护眼等）
  - 文件持久化（章节离线缓存、书籍更新检测标记）
- **构建**：Gradle Kotlin DSL；输出目录隔离为 `builds/app_N`（规避本机删除拦截）
- **最低/目标 SDK**：minSdk 26 / compileSdk & targetSdk 35

---

## 3. 架构与关键文件

```
app/src/main/java/com/xiaoswz/reader/
├── MainActivity.kt              # 入口：初始化 AppContext、CrashLogger、enableEdgeToEdge、套主题
├── ui/
│   ├── AppRoot.kt               # 路由 + 底部导航 + 外壳主题模式 + 品牌闪屏
│   ├── theme/Theme.kt           # M3 Expressive 调色板 + 大圆角形状
│   ├── components/              # 复用组件：BookCoverCard / StatusPill / SectionHeader / EmptyState / Skeleton
│   ├── bookstore/               # 书城：轮播 Banner + 状态筛选 + 热门横滑 + 网格（纯前端分组）
│   ├── bookshelf/               # 书架：最近阅读大卡 + 封面网格 + 有更新角标 + 打开时自动恢复封面
│   ├── detail/                  # 书籍详情：渐变头图 + 封面 + 数据胶囊 + 加入书架（收藏时压缩封面）
│   ├── settings/                # 设置：外观分段控件 + 检查更新 + 分组卡片
│   ├── reader/                  # 阅读页（章节正文，0.3.0 未改）
│   └── update/                  # 局域网更新检查（UpdateDialog）
├── data/
│   ├── AppContext.kt            # 应用上下文持有者
│   ├── api/XiaoswzApi.kt        # 主站 API 定义
│   ├── BookRepository.kt        # 数据层：章节三级缓存（内存→文件→网络）
│   ├── model/Models.kt          # BookSummary/BookDetail/ChapterMeta + resolveCoverUrl(data URI→ByteBuffer)/shrinkCover(封面压缩)
│   ├── settings/ReaderSettings.kt   # 阅读器设置（DataStore）
│   ├── settings/AppSettings.kt     # 外壳主题模式（浅色/深色/跟随系统）
│   ├── cache/ChapterCacheManager.kt # 章节正文文件缓存
│   ├── bookshelf/BookUpdateStore.kt # 已知章节数 + 有更新标记
│   ├── bookshelf/BookshelfRepository.kt # 书架仓储：blankOversizedCovers / repairCovers（封面自动恢复）
│   ├── bookshelf/BookEntity.kt  # Room 实体（coverUrl 存压缩后缩略图 data URI）
│   └── bookshelf/BookDao.kt     # 书架 DAO：observeAll / upsert
└── ...
```

**数据模型要点**：
- `BookSummary`：列表/卡片用，`cover`(URL)、`title`、`author`、`status`(0连载/1完结)、`slug`、`lastChapter`、`updatedAt`、`wordCount`
- `BookDetail`：详情页用，`intro`、`chapterCount`、章节列表等
- `ChapterMeta`：`id`、`title`、`order`、`slug`（书源阅读用）
- **封面字段**：主站返回的 `coverImage`/`coverUrl` 是 `data:image/jpeg;base64,...` 绝对 data URI（非普通 URL）。APP 端 `resolveCoverUrl()` 负责解码渲染，`shrinkCover()` 负责压缩存储。绝不当普通 URL 处理。

---

## 4. 版本路线图（Milestone）

| 阶段 | 状态 | 内容 |
|---|---|---|
| M1 基础壳子 | ✅ | 书城/书架/详情/阅读/设置雏形 |
| M2 阅读体验 | ✅ | 阅读页完善 |
| M3 本地书架/离线/更新检测 | ✅ (v0.2.9) | Room 书架 + 章节离线缓存 + 书籍有更新角标（**不动 Room schema**） |
| 顶栏统一与壳子美化 | ✅ (v0.3.9) | 抽出共享 `AppTopBar` 统一四页；闪屏页还原原始设计 |
| 封面显示修复 | ✅ (v0.4.0–0.4.2) | Coil 不加载 data: URI → 解码为 `ByteBuffer` 交 `ByteBufferFetcher` |
| 书架崩溃修复 | ✅ (v0.4.3) | 收藏封面撑爆 SQLite 游标窗口 → 压缩存储 + 旧数据按 slug 自动恢复 |
| M4 书城增强/分类/热度榜 | ⏳ 待动后端 | 主站接口已就绪（`/api/books?category`、`/api/books/ranking`、`/api/categories`），几乎零后端风险 |
| M5 发布就绪 | ⏳ | 商店/签名/自动化 |
| 远期 | 💡 | 登录同步（需先解决 APP 登录 token 端点，主站现有表够用） |

**v0.4.3（当前，阶段性稳定存档）**：在 v0.3.x 壳子美化基础上，已修复封面显示（data URI 解码）与书架崩溃（封面压缩存储），书城浏览 / 本地书架 / 离线阅读 / 封面显示均稳定。

---

## 5. 设计系统（v0.3.0 确立，v0.4.x 沿用）

### 5.1 设计语言
- **风格**：Material 3 Expressive 现代风（鲜艳主色 + 大圆角 24–28dp + 柔和投影 + 强动效）
- **主题模式**：外壳支持「浅色 / 深色 / 跟随系统」三档（AppSettings，DataStore 持久化）
- **调色板**：更鲜艳的青蓝主色；护眼绿/纯黑仍是阅读器内部主题，与外壳解耦
- **复用组件**（新增）：`BookCoverCard`（封面+角标+进度）、`StatusPill`（连载/完结胶囊）、`SectionHeader`（分区标题）、`EmptyState`（空状态插画）、`Skeleton`（骨架屏 shimmer）

### 5.2 各屏改动
- **入口**：新增品牌闪屏（渐变 + 图标 + 淡出主页）
- **底部导航**：保留 M3 样式，设置 Tab 有更新时显示小圆点
- **书城**：封面轮播 Banner（自动轮播+指示点）→ 状态筛选胶囊（全部/连载/完结，纯前端过滤）→ 热门推荐横滑 → 最新上架网格；加载用骨架屏；下拉刷新。分区用现有列表按 `updatedAt`/`status` 前端临时分组，**不新增后端**
- **书架**：顶部最近阅读大卡（封面+进度+续读）→ 封面网格（「有更新」角标+删除）→ 空状态插画
- **详情**：渐变头图 + 封面 + 状态/字数/章节胶囊 + 醒目加入书架按钮（逻辑不变）
- **设置**：渐变版本 Hero 卡 + 外观分段控件 + 检查更新入口 + 图标化分组卡片

### 5.3 约束
- **阅读页（章节正文）全程不改**，符合约定
- 不引入新后端依赖；轮播/分区纯前端实现

### 5.4 v0.4.x 关键技术修复（封面 + 书架，纯本地代码）
- **封面显示（v0.4.0–0.4.2）**：主站封面是 `data:image/jpeg;base64,...` 绝对 data URI；Coil 的 `AsyncImage` 对 String 型 data URI 无 fetcher，会静默失败（文字正常、图全空）。`resolveCoverUrl()` 把 data URI 解码为 `ByteBuffer` 交给 Coil 的 `ByteBufferFetcher`；书城轮播/网格、书架、详情四处均调用。
- **书架崩溃（v0.4.3）**：收藏时把整串 data URI（几百KB~几MB）存进 Room 的 `cover_url` 列，单行超出 SQLite 游标窗口（约1-2MB）→ `SELECT *` 整体崩溃（`SQLiteBlobTooBigException`）。`shrinkCover()` 在收藏时把封面缩到宽≤320px、JPEG q80 重编码成缩略图（几KB~几十KB）再存；`BookshelfRepository.blankOversizedCovers()` 用原生 `UPDATE ... SET cover_url='' WHERE cover_url LIKE 'data:%'`（UPDATE 不读游标窗口，安全）清空旧大封面，再由 `repairCovers()` 按 slug 重新拉取压缩写回，启动/打开书架时自动触发（幂等，需联网）。**严守铁律：未改 Room schema（version 1 + fallbackToDestructiveMigration），不丢书架。**

---

## 6. 构建 / 部署 / 更新通道

### 6.1 构建
```bash
cd U:\xiaoswz-reader
./gradlew assembleDebug        # 输出 builds/app_N/outputs/apk/debug/app-debug.apk
```
- 输出目录隔离 `builds/app_N`（每次新建不删），绕开本机 safe-delete 删除拦截
- 构建用 Android Studio 自带 JBR（`C:\Program Files\Android\Android Studio\jbr`）

### 6.2 局域网更新通道（测试用）
```bash
# 把 APK 放到 lan-update/ 并更新 lan-update/version.json（versionCode/versionName/apkUrl/notes）
cd U:\xiaoswz-reader\lan-update
python lan_update_server.py    # 端口 8765，APP 检查更新地址 http://192.168.2.4:8765
```
- APP 在书城页打开时**自动检测更新**（`UpdateManager.check`），设置页也有手动「检查更新」入口；局域网服务器 `http://192.168.2.4:8765` 须在运行期才有效。
- 更新服务器**只在运行期间有效**，会话/重启后需重新启动

### 6.3 GitHub Release（测试人员下载）
- 仓库已公开，Release 挂 APK 资产
- 大文件（≈18MB）上传须后台执行避免超时

---

## 7. 开发铁律（重要教训）

1. **遇到 Bug 先读代码，不猜原因** —— 本地源文件是唯一真相
2. **改字段/API 前先搜全部引用**，确认影响范围
3. **涉及数据库结构变更须先审批**，不得擅自删字段/改约束/跑 migration
4. **不许"改了试试看"式部署**，每次部署前须知改了什么、为何改、影响什么
5. **用户判断优先于 AI 猜测**
6. **最小化修改永远第一选择**，能改 1 个文件解决的不改 10 个
7. **冲浪阅读是主站子项目，但任何 bug 都只可能是本地代码问题** —— 绝不靠读主站页面 / 抓线上排错；定位只靠读本地代码（对照"能成的 A"与"不成的 B"找差异）+ USB 真机调试。用户给的"A 能 B 不能"线索是排错方向不是结论。
8. **vivo 等 ROM 屏蔽第三方 App 的 logcat** —— 调试用「反射拿 `Context` 写内部文件 + `adb run-as` 拉取」绕过，不依赖 logcat。

---

## 8. 已知坑（踩过）

1. **Room `fallbackToDestructiveMigration()` 风险**：改 schema（加表/列/升 version）会**清空整个 bookshelf.db**（书架+进度全丢）。任何需持久化的新数据（缓存、标记）一律用**文件/DataStore**，不碰 Room。M3 的离线缓存/更新检测即如此实现。
2. **token 严禁入仓库**：GitHub 经典 token 只存本地非仓库文件（`~/.workbuddy/MEMORY.md`、项目交接文档），绝不提交。误提交须 `git commit --amend` + `git push --force` 清洗历史，并 `reflog expire + gc` 清悬空对象；**不能靠"改私有"兜底**。
3. **git push 在沙箱杀 shell**：用 `GIT_TERMINAL_PROMPT=0 git -c credential.helper=` 绕过 git-credential-manager 崩溃；本机没装 gh 时生成经典 `ghp_` token（勾选 `repo` 总开关）最稳。
4. **Release 创建校验失败**：勿传 `target_commitish` 短 SHA；去掉即可。
5. **大文件上传超时**：APK 用 `run_in_background=true` 后台上传。
6. **KAPT 误报 `Unresolved reference`**：常因模型字段名用错（如 `detail.slug` 不存在），改用正确字段或 Composable 入参规避。
7. **书城 `items` 导入冲突**：`lazy.items` 与 `lazy.grid.items` 同名，需别名其一（如 `lazyItems`）。
8. **渐变遮罩写法**：Compose 中不可嵌套 `Brush`；用 `Brush.verticalGradient(listOf(transparent, scrim))` 直接声明。
9. **Coil 不加载 String 型 `data:` URI**：`AsyncImage(model = "data:image/jpeg;base64,...")` 会静默失败（文字正常、图全空）。须把 data URI 解码为 `ByteBuffer` 交给 Coil 的 `ByteBufferFetcher`（`resolveCoverUrl` 已处理）。
10. **SQLiteBlobTooBigException（书架崩溃）**：Room 的 `cover_url` 列若存整串 data URI（几百KB~几MB），单行超出 SQLite 游标窗口（约1-2MB），`SELECT *` 查询整体崩溃。修复：收藏时 `shrinkCover()` 压缩成宽≤320px 缩略图（几KB~几十KB）；旧数据用原生 `UPDATE ... SET cover_url='' WHERE cover_url LIKE 'data:%'`（UPDATE 不读游标窗口，安全）清空后按 slug 重新拉取恢复。
11. **顶栏 `statusBarsPadding()` 误用**：对背景层用会推开 ~28dp 纯色空白块，视觉"两倍大"；只能加在内容 Row/Column（已抽共享 `AppTopBar` 统一四页）。

---

## 9. 账号与安全

- Vercel 部署优先于 GitHub；GitHub 经典 token（classic，`ghp_` 开头，勾 `repo`）最长 1 年，到期前需重新生成并替换本地文档。
- 密钥只存本地非仓库文件，严禁提交。

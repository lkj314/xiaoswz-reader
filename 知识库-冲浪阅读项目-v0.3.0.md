# 冲浪阅读 Android APP — 项目完整知识库

> 整理时间：2026-08-14 ｜ 适用版本：v0.3.0（壳子美化 + 主题模式 + 书城/书架/详情/设置重做）
> 目标：一份文档吃透整个项目，快速建立全局认知。

---

## 0. 一句话定位

**冲浪阅读（Surf Reader）** 是 `xiaoswz.vercel.app`（冲浪中文网）的**原生安卓第三方只读阅读客户端**。不登录、不直连数据库，所有数据来自主站公开只读 API。

---

## 1. 项目概况

| 项 | 内容 |
|---|---|
| 应用名 | 冲浪阅读（Surf Reader） |
| 包名 | `com.xiaoswz.reader` |
| 工作目录 | `U:\xiaoswz-reader`（独立 Git 仓库） |
| GitHub | `lkj314/xiaoswz-reader`（已公开，含 Release 资产） |
| 主站仓库 | `lkj314/novel-site`（私有，主站 Next.js 项目） |
| 生产站点 | https://xiaoswz.vercel.app |
| 当前版本 | v0.3.0（versionCode 9） |
| 最低/目标 SDK | minSdk 26 / compileSdk & targetSdk 35 |
| 语言/UI | Kotlin + Jetpack Compose (Material 3) |

**核心约束**：APP 不登录、不直连数据库，所有数据来自主站公开只读 API（`XiaoswzApi`）。任何持久化新数据都走文件/DataStore，**不碰 Room schema**。

---

## 2. 技术栈

- **UI**：Kotlin + Jetpack Compose（Material 3）+ Compose BOM `2024.12.01`
- **导航**：Navigation Compose（路由式）
- **网络**：Retrofit + OkHttp + kotlinx.serialization（JSON 解析，`ignoreUnknownKeys`）
- **图片**：Coil（`material-icons-extended` 已含，图标充足）
- **本地持久化（三层）**：
  - **Room 2.6.1**（仅本地书架 + 阅读进度，用 KAPT 插件）—— **严禁改 schema**
  - **DataStore**（设置：阅读器设置 `reader_settings` + 外壳主题 `app_settings`）
  - **文件**（章节离线缓存 `chapter_cache/`、书籍更新标记 `book_update.json`、更新 APK `updates/`）
- **构建**：Gradle Kotlin DSL；输出目录隔离为 `builds/app_N`（规避本机删除拦截）
- **构建 JDK**：Android Studio 自带 JBR（`C:\Program Files\Android\Android Studio\jbr`）

---

## 3. 整体架构

```
┌──────────────────────── 表现层 (ui/) ────────────────────────┐
│  AppRoot(路由+底栏+闪屏) → Bookstore / Bookshelf / Settings    │
│       ↓ 点击                         ↓ 点击                     │
│  BookDetailScreen ──→ ReaderScreen（章节正文，0.3.0 未改）       │
└───────────────────────────────────────────────────────────────┘
                          │ 调用
                          ▼
┌──────────────────────── 数据层 (data/) ──────────────────────┐
│  BookRepository（三级缓存：内存→文件→网络）                    │
│  ├─ ApiClient → XiaoswzApi（Retrofit，主站只读 API）          │
│  ├─ ChapterCacheManager（章节文件缓存）                        │
│  └─ BookUpdateStore（更新标记，JSON 文件）                     │
│  AppSettingsRepository / ReaderSettingsRepository（DataStore） │
│  UpdateManager（局域网更新：拉 version.json→下载→安装）         │
└───────────────────────────────────────────────────────────────┘
                          │ HTTP
                          ▼
              冲浪中文网公开 API (https://xiaoswz.vercel.app)
```

**设计要点**：UI 不直接碰网络，统一经 `BookRepository`；Repository 负责缓存策略；持久化按"是否能容忍丢失"分层（Room 仅书架、DataStore 设置、文件 缓存/标记）。

---

## 4. 目录结构与关键文件

```
app/src/main/java/com/xiaoswz/reader/
├── MainActivity.kt
│     # 入口：AppContext.init(application)、CrashLogger.install、enableEdgeToEdge
│     # 套 SurfReaderTheme，渲染 AppRoot()
├── ui/
│   ├── AppRoot.kt            # 路由表 Routes + 底部导航 + 外壳主题模式 + 品牌闪屏 + 页面转场
│   ├── theme/Theme.kt       # SurfReaderTheme：M3 Expressive 调色板 + 大圆角形状
│   ├── components/           # 复用组件（v0.3.0 新增）
│   │   ├── BookCoverCard.kt  # 封面卡 + 状态角标 + 进度条
│   │   ├── StatusPill.kt     # 连载/完结 胶囊
│   │   ├── SectionHeader.kt  # 分区标题（图标 + 文字）
│   │   ├── EmptyState.kt     # 空状态插画（图标 + 文案 + 可选动作）
│   │   └── Skeleton.kt       # 骨架屏 shimmer（加载占位）
│   ├── bookstore/            # 书城：轮播 Banner + 状态筛选 + 热门横滑 + 网格 + 下拉刷新
│   ├── bookshelf/            # 书架：最近阅读大卡 + 封面网格 + 有更新角标 + 空状态
│   ├── detail/               # 书籍详情：渐变头图 + 封面 + 数据胶囊 + 加入书架
│   ├── settings/             # 设置：外观分段控件 + 检查更新 + 分组卡片
│   ├── reader/               # 阅读页（章节正文，M2 完成，0.3.0 不改）
│   └── update/UpdateDialog.kt# 局域网更新对话框（检查/下载/安装进度）
├── data/
│   ├── AppContext.kt        # 应用上下文持有者（全局取 filesDir 等）
│   ├── api/XiaoswzApi.kt     # 主站 API 接口 + ApiClient（Retrofit 单例）
│   ├── BookRepository.kt     # 数据仓库：章节三级缓存 + 详情内存缓存 + 预取
│   ├── model/Models.kt       # 所有 DTO + formatWordCount 格式化
│   ├── settings/
│   │   ├── ReaderSettings.kt # 阅读器设置（DataStore，4 套主题/字号/行距/翻页…）
│   │   └── AppSettings.kt    # 外壳主题模式（浅色/深色/跟随系统）
│   ├── cache/ChapterCacheManager.kt  # 章节正文文件缓存（读/写/大小/清空）
│   ├── bookshelf/BookUpdateStore.kt # 已知章节数 + 有更新标记（JSON 文件）
│   └── update/UpdateManager.kt       # 局域网更新：check/downloadApk/installApk
└── ...
```

---

## 5. 数据模型（Models.kt）

所有字段均可空（服务端可能缺字段），解析用 `ignoreUnknownKeys`。

### 5.1 书城列表 `GET /api/books` → `BookListResponse`
- `books: List<BookDto>`
- `BookDto`：`id` `slug` `title` `coverImage` `status`(`"ONGOING"/"COMPLETED"/"HIATUS"/"DROPPED"`) `wordCount` `chapterCount` `displayHeat` `updatedAt` `author`(AuthorDto) `category`(CategoryDto)
  - `displayAuthor`：优先笔名 → 真名 → authorName → "佚名"
  - `statusText`：ONGOING→连载中 / COMPLETED→已完结 / HIATUS→暂停中 / DROPPED→已切书

### 5.2 书籍详情 `GET /api/book-source?action=detail` → `BookDetailDto`
- `id` `name` `author` `coverUrl` `intro` `wordCount` `status` `chapterCount` `chapters: List<ChapterDto>`
- `ChapterDto`：`id` `name` `wordCount` `url` `index`

### 5.3 章节正文 `GET /api/book-source?action=content` → `ChapterContentDto`
- `id` `title` `content`(纯文本，服务端已解压) `bookName` `bookSlug`

### 5.4 通用工具
- `formatWordCount(count)`: `12345 → "1.2万字"`（≥1万转万，≥100万取整）

---

## 6. API 端点（XiaoswzApi.kt）

| 端点 | 方法/参数 | 返回 | 说明 |
|---|---|---|---|
| `/api/books` | GET `page` `limit=24` `sort=latest\|popular` `search?` | `BookListResponse` | 书城列表，分页 |
| `/api/book-source?action=detail&bookId={slug}` | GET | `BookDetailDto` | 详情 + 完整目录 |
| `/api/book-source?action=content&chapterId={id}` | GET | `ChapterContentDto` | 章节正文 |

- Base URL：`BuildConfig.API_BASE_URL = "https://xiaoswz.vercel.app"`
- 超时：连接 15s / 读取 30s；DEBUG 开 BASIC 日志
- **全部只读，无需登录/鉴权**

---

## 7. 关键数据流

### 7.1 章节三级缓存（M3，断网可读）
`BookRepository.getChapterContent(chapterId)`：
1. 内存 `contentCache` 命中 → 返回（最快）
2. 文件 `ChapterCacheManager.get` 命中 → 返回 `fromOfflineCache=true`
3. 网络拉取 → 写入内存 + 文件 → 返回
> 已读章节杀进程重进、断网仍可读取。`prefetchChapter` 提前落文件，翻章零等待。

### 7.2 书籍更新检测（M3）
`BookUpdateStore`（JSON 文件，非 Room）：
- 加入书架 `setKnown(slug, count)` 记已知章节数
- 打开书籍比对服务端 `chapterCount`，更大则 `markUpdated(slug)` → 书架显示「● 有更新」
- 打开阅读后 `markSeen(slug, currentCount)` 清除标记并刷新已知数

### 7.3 局域网更新（测试通道）
`UpdateManager`：`GET {server}/version.json` → 比对 `versionCode` → 下载 APK 到 `updates/` → FileProvider 调系统安装器（需"安装未知应用"授权）。
- 默认服务器：`BuildConfig.DEFAULT_UPDATE_SERVER = "http://192.168.2.4:8765"`
- 触发：**手动**（设置→检查更新，无开机自动检测）

---

## 8. 路由与导航（AppRoot.kt）

| 路由 | 含义 | 底栏 |
|---|---|---|
| `bookstore` | 书城（起始页） | ✅ |
| `bookshelf` | 书架 | ✅ |
| `settings` | 设置 | ✅ |
| `detail/{slug}` | 书籍详情（slug 需 URL 编码） | ❌ 覆盖式 |
| `reader/{bookSlug}/{chapterId}` | 阅读页 | ❌ 覆盖式 |

- `TopLevelRoutes` = {bookstore, bookshelf, settings}，仅这三屏显示底部导航
- 页面转场：横向滑动 + 淡入（书城→详情→阅读 不再硬切），300ms `FastOutSlowInEasing`
- **品牌闪屏**：渐变背景 + `AutoStories` 图标 + "冲浪阅读/畅读每一页"，约 1.1s 后淡出进主页
- slug 含中文，必须用 `Uri.encode` 后再拼路由

---

## 9. 主题系统（两套，互不干扰）

### 9.1 外壳主题（AppSettings，v0.3.0 新增）
DataStore `app_settings` 存 `theme_mode`：`SYSTEM(0)/LIGHT(1)/DARK(2)`。
`AppRoot` 读取后套 `SurfReaderTheme(darkTheme=...)`。M3 Expressive 调色板（鲜艳青蓝 + 大圆角 24–28dp + 柔和阴影）。

### 9.2 阅读器内部主题（ReaderSettings，M2）
`themeIndex`：`THEME_DAY(0)/EYE_GREEN(1)/NIGHT(2)/BLACK(3)`（浅色/护眼绿/夜间/纯黑）。**仅作用于阅读页**，与外壳主题解耦。

> 注意两层主题独立：外壳是 app 壳子的明暗，阅读器是章节正文的背景配色。

---

## 10. v0.3.0 壳子美化设计系统

**风格**：Material 3 Expressive 现代风（鲜艳主色 + 大圆角 + 柔和投影 + 强动效）。**阅读页全程不改**。

### 10.1 复用组件（新增）
- `BookCoverCard`：封面 + 状态角标（连载/完结）+ 进度条 + 点击
- `StatusPill`：连载中/已完结 胶囊（彩色）
- `SectionHeader`：分区标题（小图标 + 文字）
- `EmptyState`：空状态插画（图标 + 文案 + 可选动作按钮）
- `Skeleton`：骨架屏 shimmer（加载占位，替代转圈）

### 10.2 各屏改动
- **书城**：封面轮播 Banner（自动轮播 + 指示点，取前若干本）→ 状态筛选胶囊（全部/连载/完结，纯前端过滤）→ 热门推荐横滑（`displayHeat` 排序）→ 最新上架网格（3 列，`updatedAt` 排序）；加载用骨架屏；支持下拉刷新（M2 material `pullrefresh`）。**分区纯前端分组，不新增后端**。
- **书架**：顶部「最近阅读」大卡（封面 + 续读按钮 + 进度）→ 下方封面网格（「有更新」角标 + 删除）→ 空状态插画。
- **详情**：封面主色生成的渐变头图 + 封面 + 状态/字数/章节胶囊 + 醒目「加入书架」按钮（逻辑不变）。
- **设置**：渐变版本 Hero 卡 + 外观分段控件（浅色/深色/跟随系统）+ 检查更新入口 + 图标化分组卡片。
- **入口**：新增品牌闪屏；底部导航保留 M3 样式。

---

## 11. 各页面职责速查

| 页面 | 文件 | 做什么 |
|---|---|---|
| 书城 | `bookstore/BookstoreScreen.kt` + `BookstoreViewModel` | 拉书城列表、搜索、状态筛选、分区展示、下拉刷新 |
| 书架 | `bookshelf/BookshelfScreen.kt` | Room 取书架、最近阅读、更新角标、删除 |
| 详情 | `detail/BookDetailScreen.kt` | 详情+目录、加入/移除书架、检测更新、跳阅读 |
| 阅读 | `reader/ReaderScreen.kt` + `ReaderViewModel` | 章节正文渲染、翻页、字号/主题/进度持久化（0.3.0 不改） |
| 设置 | `settings/SettingsScreen.kt` | 外观模式、阅读设置入口、检查更新、关于 |
| 更新 | `update/UpdateDialog.kt` + `data/update/UpdateManager` | 局域网更新全流程 |

---

## 12. 构建 / 部署 / 更新通道

### 12.1 构建
```bash
cd U:\xiaoswz-reader
export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"
export PATH="$JAVA_HOME/bin:$PATH"
./gradlew assembleDebug
# 输出 builds/app_N/outputs/apk/debug/app-debug.apk
```
- 构建用 Android Studio 自带 JBR（沙箱默认无 JAVA_HOME，必须显式设置）
- 输出目录隔离 `builds/app_N`（每次新建不删），绕开本机 safe-delete 删除拦截

### 12.2 局域网更新通道（测试用）
```bash
# 1) 把 APK 复制到 lan-update/ 并改 lan-update/version.json（versionCode/versionName/apkUrl/notes）
# 2) 启动服务器（单文件，无需依赖）
cd U:\xiaoswz-reader\lan-update
python lan_update_server.py    # 端口 8765，APP 检查更新地址 http://192.168.2.4:8765
```
- APP 内更新**手动触发**（设置→检查更新，无开机自动检测）
- 更新服务器**仅在运行期间有效**；会话结束/重启后需重跑

### 12.3 GitHub Release（测试人员下载）
- 仓库已公开，Release 挂 APK 资产；大文件（≈18MB）上传须后台执行避免超时

---

## 13. 版本路线图（Milestone）

| 阶段 | 状态 | 内容 |
|---|---|---|
| M1 基础壳子 | ✅ | 书城/书架/详情/阅读/设置雏形 |
| M2 阅读体验 | ✅ | 阅读页完善（4 套主题/字号/行距/翻页/进度） |
| M3 本地书架/离线/更新检测 | ✅ (v0.2.9) | Room 书架 + 章节离线缓存 + 书籍有更新角标（**不动 Room schema**） |
| **v0.3.0 壳子美化** | ✅ | 纯前端 Material 3 Expressive 焕新 + 外壳主题模式（不碰阅读页/后端） |
| M4 书城增强/分类/热度榜 | ⏳ 待动后端 | 需主站接口配合（分类、榜单） |
| M5 发布就绪 | ⏳ | 商店上架/签名/自动化 |
| 远期 | 💡 | 登录同步 |

---

## 14. 开发铁律（重要教训）

1. **遇到 Bug 先读代码，不猜原因** —— 本地源文件是唯一真相。
2. **改字段/API 前先搜全部引用**，确认影响范围。
3. **涉及数据库结构变更须先审批**，不得擅自删字段/改约束/跑 migration。
4. **不许"改了试试看"式部署**，每次部署前须知改了什么、为何改、影响什么。
5. **用户判断优先于 AI 猜测**。
6. **最小化修改永远第一选择**，能改 1 个文件解决的不改 10 个。

---

## 15. 已知坑（踩过，避免重蹈）

1. **Room `fallbackToDestructiveMigration()` 风险**：改 schema（加表/列/升 version）会**清空整个 bookshelf.db**（书架+进度全丢）。任何需持久化的新数据（缓存、标记）一律用**文件/DataStore**，不碰 Room。M3 的离线缓存/更新检测即如此实现。
2. **token 严禁入仓库**：GitHub 经典 token 只存本地非仓库文件（`~/.workbuddy/MEMORY.md`、项目交接文档）。误提交须 `git commit --amend` + `git push --force` 清洗历史，并 `reflog expire + gc` 清悬空对象；**不能靠"改私有"兜底**。
3. **git push 在沙箱杀 shell**：用 `GIT_TERMINAL_PROMPT=0 git -c credential.helper=` 绕过 git-credential-manager 崩溃；本机没装 gh 时生成经典 `ghp_` token（勾选 `repo` 总开关）最稳。
4. **Release 创建校验失败**：勿传 `target_commitish` 短 SHA；去掉即可。
5. **大文件上传超时**：APK 用 `run_in_background=true` 后台上传。
6. **KAPT 误报 `Unresolved reference`**：常因模型字段名用错（如 `detail.slug` 不存在），改用正确字段或 Composable 入参规避。
7. **书城 `items` 导入冲突**：`lazy.items` 与 `lazy.grid.items` 同名，需别名其一（如 `lazyItems`）。
8. **Compose 渐变遮罩**：不可嵌套 `Brush`；用 `Brush.verticalGradient(listOf(transparent, scrim))` 直接声明。
9. **M3 无 `PullToRefreshBox`**：本工程 material3 是 1.3.1（BOM 2024.12.01），该 API 在 1.4+ 才有。下拉刷新改用 M2 `androidx.compose.material.pullrefresh`（加依赖 + `@OptIn(ExperimentalMaterialApi::class)`）。
10. **构建需 JAVA_HOME**：沙箱 Bash 默认无 Java，必须 `export JAVA_HOME` 指向 Android Studio JBR 后构建。

---

## 16. 账号与安全

- Vercel 部署优先于 GitHub；GitHub 经典 token（classic，`ghp_` 开头，勾 `repo`）最长 1 年，到期前需重新生成并替换本地文档（项目交接文档、`~/.workbuddy/MEMORY.md`）。
- **密钥只存本地非仓库文件，严禁提交**。
- 本机推送命令（git-credential-manager 会崩，绕过）：
  `GIT_TERMINAL_PROMPT=0 git -c credential.helper= push https://lkj314:TOKEN@github.com/lkj314/xiaoswz-reader.git`

---

## 17. 快速上手清单（新人/接手必读）

1. 环境：装 Android Studio（自带 JBR）、配 `JAVA_HOME`、装 Git。
2. 构建：`export JAVA_HOME=...jbr && ./gradlew assembleDebug`。
3. 真机调试：USB 装（vivo 开发者模式 + `adb install -r`）；闪退先 `adb logcat -d -b crash` 抓堆栈。
4. 测试更新：把 APK 放 `lan-update/`、改 `version.json`、跑 `lan_update_server.py`，手机设置→检查更新。
5. 改 UI：先看 `ui/theme/Theme.kt` 与 `ui/components/*`，复用组件而非重复造。
6. 加持久化：能用文件/DataStore 就别动 Room。
7. 推代码：先本地提交，验证通过后 `GIT_TERMINAL_PROMPT=0 ...` 推送（token 在本地文档，勿提交）。

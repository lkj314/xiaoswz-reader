# 冲浪阅读 0.4.0 规划候选需求清单（待审）

> 状态：草稿，供用户审阅拍板，暂未动工。
> 版本定位：0.4.0 = 0.3.9 之后的下一个功能版本（versionCode 需升至 19）。
> 核心约束：App 只调主站（xiaoswz.vercel.app）公开只读 API，**暂不做登录**。任何需要后端写入/鉴权的功能一律不进 0.4.0。

---

## 一、背景与现状

- 0.3.9 已发布并推 GitHub：共享 `AppTopBar` 全站统一、海浪 logo、书城内容组件化、沉浸主题。
- 路线图（MEMORY）：M2✅ → M3 本地书架/离线缓存/进度记忆✅ → **M4 书城增强/分类/热度榜（需动后端）** → M5 发布就绪 → 远期登录同步。
- 当前已有能力：书城列表、书城详情、本地书架(Room)、阅读页（含章节离线缓存/进度记忆）、设置页、分类？搜索？（需核对主站公开 API 是否暴露）。

**关键规划原则**：0.4.0 优先选**纯客户端、零后端依赖**的项，保证能独立交付、不被后端卡住。需要后端的 M4 增强列为"若后端就绪则做 / 推迟 0.5.0"。

---

## 二、候选需求（按主题分组）

工作量标注：S（<1 天） / M（1–3 天） / L（>3 天）。🔗 = 需要主站后端配合。

### A. 阅读体验打磨（纯客户端，推荐优先）
| 项 | 描述 | 工作量 | 后端 |
|---|---|---|---|
| A1 阅读排版可调 | 阅读页行距/段距/左右边距/字号档位可调，设置可记忆 | M | 否 |
| A2 翻页模式 | 覆盖翻页 / 滚动 / 仿真三种模式切换 | M | 否 |
| A3 进度记忆强化 | 书架"继续阅读"入口、上次阅读章节高亮、跨设备无关 | S | 否 |
| A4 夜间/护眼模式 | 跟随系统或手动切换，阅读页独立主题 | S–M | 否 |
| A5 离线缓存管理 UI | 当前已有章节离线缓存逻辑，但无管理界面；新增"已缓存/清理"入口 | M | 否 |

### B. 书架与本地管理（纯客户端）
| 项 | 描述 | 工作量 | 后端 |
|---|---|---|---|
| B1 书架分组/排序 | 按更新时间/最近阅读/自定义分组，书架内搜索 | M | 否 |
| B2 书籍更新检测 UI | 已有检测逻辑（v0.2.9），缺"发现 N 本更新"入口与小红点 | S–M | 否 |
| B3 书架导入/导出 | 本地 JSON 文件备份与恢复（不碰 Room schema，用文件/DataStore） | M | 否 |

### C. 设置与个性化（纯客户端）
| 项 | 描述 | 工作量 | 后端 |
|---|---|---|---|
| C1 主题色自定义 | 当前主色固定 primary，开放 2–3 套预设或取色 | M | 否 |
| C2 数据/缓存管理页 | 清理图片缓存、离线章节、重置设置 | S | 否 |
| C3 关于页完善 | 版本号/更新日志/检查更新入口整合（当前散落） | S | 否 |

### D. 书城增强 M4（🔗 需后端，谨慎排期）
| 项 | 描述 | 工作量 | 后端 |
|---|---|---|---|
| D1 分类/频道 Tab | 玄幻/都市/网文等分栏，需主站分类 API 或本地硬编码映射 | L | 🔗 是 |
| D2 搜索 | 书城内搜索，需主站搜索 API（书源搜索结果组件已存在，需确认公开接口） | M–L | 🔗 是 |
| D3 热度榜/推荐位 | 首页 banner + 热榜/编辑推荐，需主站榜单 API | M | 🔗 是 |

### E. 工程质量（可选，按需）
| 项 | 描述 | 工作量 | 后端 |
|---|---|---|---|
| E1 技术债梳理 | 0.3.x 快速迭代积累的代码债（顶栏债已记于 TOPBAR-POSTMORTEM.md）集中清理 | M | 否 |
| E2 Release 构建 | 当前只发 debug APK；补签名配置 + release 构建 + GitHub Release 资产 | M | 否 |
| E3 基础测试 | 关键 Composable / 数据层起步单元测试 | L | 否 |

---

## 三、后端 API 缺口（D 类前置条件）

D1/D2/D3 能否做，取决于主站公开只读 API 是否已暴露：
- 分类列表、搜索、榜单接口是否存在且对 App 开放？
- 书源搜索结果组件（`search-results.tsx`）已支持语义类名，但那是**爬虫式搜索**，非 App 直调的 JSON API。

**建议**：在排期 D 类前，先确认主站 API 现状（可让主站侧或你本人核查），否则 D 类容易卡死。

---

## 四、初版范围建议（待你拍板）

**推荐 0.4.0 聚焦：A + B + C 中的纯客户端项**，理由：
1. 全部零后端依赖，可独立交付、不阻塞。
2. 直接提升"看书"这条主链路体验（排版/翻页/进度/书架管理），用户感知强。
3. D 类（书城增强）依赖后端，建议作为 **0.5.0** 目标或"后端就绪后插入"。

**候选首发包（A+B+C 精选，约 1–2 周工作量）**：
- A1 阅读排版可调
- A3 进度记忆强化
- A5 离线缓存管理 UI
- B2 书籍更新检测 UI
- C2/C3 数据管理页 + 关于页整合

> 上述仅为建议，最终范围由你圈定。

---

## 五、下一步

1. 你从上面清单圈定 0.4.0 范围（可增删、可调整分组）。
2. 我据此拆任务、出详细设计（每个入选项的 UI/数据结构/改动文件）。
3. 确认后动工，按惯例构建 → 局域网更新通道验证 → 推 GitHub。

# 冲浪阅读 · 项目版本日志（脉络）

> 一份"演进史"，用来快速理解项目是怎么长到今天这个样子的。
> 当前最新：**安卓 0.15.7（code 83）/ 后端随版本号同步**。
> 配套文档：`冲浪阅读项目交接文档.txt`（骨架、文件导航、功能清单、铁律、发布操作）。

---

## 〇、三个仓库的关系（必读）

| 仓库 | GitHub | 可见性 | 角色 | 状态 |
| --- | --- | --- | --- | --- |
| **主站 novel-site** | `lkj314/novel-site` | private | 冲浪中文网（小说正文/章节/书源 API），域名 `xiaoswz.vercel.app` | **归档于 v8.8.1，永不改动**（最高红线） |
| **安卓 App** | `lkj314/xiaoswz-reader` | **public** | 阅读器本体（Kotlin + Compose），走 GitHub raw 分发 APK | 活跃 |
| **后端** | `lkj314/xiaoswz-reader-backend` | private | 账号/云同步/评论评分/榜单/广告/管理后台（Next15 + Prisma + Neon） | 活跃 |

**关键真相**：安卓从**主站**取小说正文/章节（只读 API，`API_BASE_URL=xiaoswz.vercel.app`），从**独立后端**取业务数据（评论/书架/进度/书友圈，`BACKEND_BASE_URL=xiaoswz-reader-backend.vercel.app`）。后端**不存任何小说正文**（物理隔离，正文只活在书源 API 里）。

---

## 一、时代分期（一图看懂）

```
M1 脚手架 ─┐
M2 匿名账号 ─┤ 后端奠基 (v0.5.9~0.6.x)
M3 云端同步 ─┘
                │
模块A 标注/书签/搜索 (0.11.0) ─┐
模块B 听书TTS      (0.11.3)     │
模块C 护眼显示      (0.11.4)     ├ 能力模块期 (0.11.x)
模块D 社区分享      (0.11.5)     │
核心去脆弱化        (0.11.2) ────┘
                │
创意工坊 v1       (0.12.0) ─── 插件化 (0.12~0.13)
                │
角色互动          (0.14.0) ─┐
创作者中心(内嵌)   (0.14.1)  ├ 社区/运营期 (0.14.x)
                │
章评段评地基      (0.15.0) ─┐
讨论氛围地基      (0.15.2)   │
段评独立全屏页    (0.15.3/4) ├ 讨论氛围期 (0.15.x)
气泡挡字修复      (0.15.5)   │
更新通道修复      (0.15.6)   │
废除气泡系统      (0.15.7) ──┘
```

---

## 二、详细版本脉络

### 阶段 0 · 起点与后端奠基（0.5.x → 0.6.x）
- **0.5.4~0.5.8**：iOS 浅色玻璃风 UI 统一改造、启动页全屏化、健康治理（清理死资源/调试日志收口）。
- **0.6.0 M3 开端**：云端同步打通，后端上线 Vercel，App 直连云端同步（`GET /api/sync`，后端 LWW 冲突策略）。
- **0.6.1**：后端正式上线（Neon 数据库），App 直连。
- **0.6.2**：默认更新源切云端（Vercel 独立项目 `public/` CDN）。
- **0.6.3**：云端更新通道迁 **GitHub raw + Releases**（绕开 vercel.app 域名污染历史问题）。
- **0.6.6**：流量平台基础（P1/P2/P3 上云 + 评论基础风控）。

### 阶段 1 · 社区/书城/用户中心成型（0.7.x → 0.10.x）
- **0.7.0**：管理台一键直达；**0.7.4~0.7.9** 书单/社区/成就/运营位全链路。
- **0.8.0**：书单内加书组件 + UID 书号识别 + 独立用户中心；**0.8.1** 自有书库（`BookCatalog` 轻量表 + 封面代理）；**0.8.2** 书城拆"首页 + 书库"（首页=Hero+热度轮播；书库=搜索+分区+榜单）。
- **0.9.0**：0.8 适配线收官。**0.10.0/0.10.1**：0.9.x 缓存/预读线收官 + 无缝连续阅读改造。

### 阶段 2 · 能力模块期（0.11.x）—— 产品功能大爆发
- **0.11.0 模块A**：标注/书签/书内搜索 + 跨设备云端同步（后端 `annotations` 表 + API）。
- **0.11.1**：热修——恢复滚动 + 删除书签（回归修复）。
- **0.11.2 核心去脆弱化**：移出自研选区/划线/标注面板 UI，永保滚动体验（**重要架构决策**：核心只做纯文本渲染+滚动/翻页+书内搜索，增强能力改以"创意工坊插件"形式开放）。
- **0.11.3 模块B**：听书 TTS（逐句朗读 + 章末自动续读 + 语速 0.5~2.0x）。
- **0.11.4 模块C**：护眼显示（蓝光过滤 + 连续阅读休息提醒）。
- **0.11.5 模块D**：社区分享（阅读内一键分享书摘到书友圈）。
- **0.11.6**：听书修复（暂停续读 + 当前句锚点高亮）。

### 阶段 3 · 插件化 / 创意工坊（0.12.x → 0.13.x）
- **0.12.0 创意工坊 v1**：插件广场四 Tab + 划词标注 + decorator 锚点渲染。
- **0.12.1**：修复创意工坊打开即测量崩溃。
- **0.13.0**：书签合集页 + 插件可点开/停用 + 制作 Tab 自定义 + 划词标注；**KSP 迁移**（替代 kapt，修 Kotlin 2.0 下编译错误吞没）。
- **0.13.1**：移除置顶离线缓存警示条（目录缓存章置灰+缓存标）；听书支持选中段落从此处开始听。
- **0.13.2**：在线更新机制落地——`lan-update/version.json` 升版本 + 同源 APK（App 检查更新实际读取源）；听书选区工具条常驻所有阅读模式。

### 阶段 4 · 社区/角色/运营（0.14.x）
- **0.14.0 角色互动**：书籍可自定义热门角色；读者互动三件套=比心（可取消）/标签墙（共建+投票去重）/角色讨论（复用 Comment 表）。
- **0.14.1 创作者中心**：管理功能**内嵌 App**（替代存在感低的独立网页后台）——角色录入 / 书籍元数据编辑 / 公告管理，仅 admin 可见。

### 阶段 5 · 讨论氛围期（0.15.x）—— 当前主线
- **0.15.0 章评段评**：评论体系地基。**正文不入库**；评论锚定章节（章评）或段落+段内偏移+引用快照（段评）。后端 `Comment` 表扩展章锚/段锚字段。
- **0.15.1 段评全模式 + 内容寻址**：修复默认连续滚动下划词"评"工具条不出现；段评落锚改为**内容寻址**（引用文本自身定位，容忍书源段落重排）；后端 `serverExternalPackages` 修复 Prisma 路由 500（章评评论失败）。
- **0.15.2 讨论氛围地基**：段评视觉降级（极淡珊瑚6%+下划线，消除"始终被选中"重高亮）+ 段落末 `💬 N` 气泡入口 + 阅读器设置「段评标记」沉浸式开关 + 章评/段评 sheet 互链 + 楼中楼（后端 `Comment.parentId` 已就位）。
- **0.15.3 段评独立全屏页**：跨章节聚合全书根段评（起点/番茄风格讨论流）。代码就位，**随 0.15.4 一并发布**。后端新增 `/api/books/{src}/{id}/segment-comments` 聚合端点。
- **0.15.4 书友圈评论框修复 + 0.15.3 全屏页发布**：修复 `PostDetailSheet` 评论输入框被键盘盖住（`Column` 缺 `imePadding()`，叠加 `enableEdgeToEdge()`）。
- **0.15.5 段评气泡挡字修复**：气泡 anchor 改为贴段评段落右外侧（Text `end-padding` 区域内），垂直对齐末字符行中线，强制不挡字。
- **0.15.6 更新通道修复（重要）**：① 补发 `0.15.4.apk`（此前漏发导致版本线断裂，用户拉到 0.15.4 下载 404）；② `UpdateManager` 加多通道 fallback（raw → jsDelivr CDN 镜像），治本 raw 国内偶发超时。
- **0.15.7 废除段评气泡系统**：彻底移除段落末 `💬 N` 气泡、正文右侧 56dp 预留 padding 与段评下划线/底色标记，正文恢复完整填充；段评入口收敛到阅读器菜单「段评/全部段评」与长按选区「评」工具条；`UpdateManager` 缩短超时并每通道重试 2 次，fallback 更积极。

---

## 三、当前最新状态（0.15.7）一句话总结

安卓 0.15.7（code 83）已发布并推送到 GitHub raw；后端未改。段评气泡系统彻底下线，正文排版回归干净完整；章评/段评/楼中楼/跨章聚合讨论流等功能全部保留，入口改到菜单与选区工具条。

**仍在打磨的小点**（供后续版本）：`SegmentThreadSheet` 复用后的回复/点赞回归验证；验证 0.15.2 真机是否能通过 raw/jsDelivr 双通道成功 OTA（若仍失败则需手动 USB 安装一次 0.15.7）。

---

## 四、怎么读这份日志

- 想看"现在有哪些功能" → 看 `冲浪阅读项目交接文档.txt` 的「已实现功能清单」。
- 想看"要改某个功能去哪个文件夹" → 看同文档的「文件导航（做什么去哪找）」。
- 想看"发布一个版本要做什么" → 看同文档的「构建 / 发布 / 更新」。
- 想看"哪些绝对不能碰" → 看同文档的「关键铁律 / 红线」。

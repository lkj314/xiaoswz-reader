# ANIBUZZ 风格页面框架设计文档 v1.0

> 基于 ANIBUZZ 视觉拆解报告，为「冲浪阅读」定制的二次元沉浸式 UI 框架。
> **本阶段只定义框架规范，不涉及具体角色图放置。**

---

## 一、ANIBUZZ 核心设计语言

### 1.1 四大视觉原则

| 原则 | 含义 | 落地要求 |
|------|------|---------|
| **Dark Immersive Base** | 深色沉浸基底 | 整个 App 使用深海蓝色系作为页面底色，非纯白 |
| **Character = focal point** | 角色是视觉焦点 | 角色不是装饰贴纸，而是有专门设计的"舞台" |
| **Glass-morphism Cards** | 玻璃态卡片 | 半透明、低对比度、微模糊的卡片容器 |
| **Low contrast layers** | 低对比度层级 | 元素之间对比柔和，不刺眼 |

### 1.2 颜色系统

```
┌─────────────────────────────────────────────┐
│  Deep Ocean    #0A1628  页面基底（最深）     │
│  Surface       #142836  卡片/容器底色        │
│  Surface Light #1E3A5F  悬停/高亮表面        │
│  CTA Blue      #4A90D9  行动按钮/链接        │
│  Text Primary  #E8F0FC  主文字（近白）        │
│  Text Muted    #8BA3C7  辅助说明文字          │
│  Accent Pink   #E87DBF  强调/爱心/通知角标    │
│  Whale Blue    #5B9FDA  品牌色（鲸鱼蓝）      │
└─────────────────────────────────────────────┘
```

### 1.3 ANIBUZZ 三屏结构分析

#### Screen 1: Splash（启动页）
```
┌──────────────────────┐
│                      │
│    アニバズ           │  ← 小字品牌（日文）
│    ANIBUZZ           │  ← 大字品牌名
│                      │
│   ┌────────────┐     │
│   │            │     │  ← 椭圆角色框
│   │  Character │     │     (Hero Full-body)
│   │   Hero     │     │
│   │            │     │
│   └────────────┘     │
│                      │
│      ─── ● ──        │  ← 进度指示器
└──────────────────────┘
```
**特点**：角色是唯一焦点，居中放大，有椭圆形"画框"容器。

#### Screen 2: Welcome（欢迎引导页）
```
┌──────────────────────┐
│  ┌────────────────┐  │
│  │                │  │
│  │  Character     │  │  ← 角色特写（40%屏幕）
│  │  Close-up      │  │     (emotional/eye contact)
│  │                │  │
│  ├────────────────┤  │
│  │ The fun is     │  │  ← 主文案
│  │ already calling│  │
│  │ you inside!    │  │
│  │ What are you   │  │  ← 副文案
│  │ waiting for?   │  │
│  │  ┌──────────┐  │  │
│  │  │Get Started│  │  │  ← CTA 按钮（蓝色圆角）
│  │  └──────────┘  │  │
│  └────────────────┘  │
│       新世界          │  ← 底部辅助文字
└──────────────────────┘
```
**特点**：整个欢迎区域是一个**统一的深色卡片**，角色在上、文字在下，一体设计。

#### Screen 3: Feed（信息流主页）
```
┌──────────────────────┐
│ Hey Takashi          │  ← 问候语
│ [All] [Mentions] [∵] │  ← 标签筛选栏
│                      │
│ ┌──────────────────┐ │
│ │ ○ Nico Taylor    │ │  ← 用户头像+名字
│ │                  │ │
│ │  ┌────────────┐  │ │
│ │  │ Character  │  │ │  ← 卡片内角色插图
│ │  │ Post Image │  │ │
│ │  └────────────┘  │ │
│ │ ❤ 💬 🔗 📋 📌   │ │  ← 互动工具栏
│ └──────────────────┘ │
│                      │
│ 🔍  👤  ☰           │  ← 底部导航
└──────────────────────┘
```
**特点**：角色图在卡片**内部**，卡片本身是玻璃态容器；不是角色飘在外面。

---

## 二、冲浪阅读页面映射

### 2.1 页面对照表

| ANIBUZZ 概念 | 冲浪阅读页面 | 当前状态 | 目标状态 |
|-------------|-------------|---------|---------|
| Splash | SplashScreen | ✅ B站风格全屏启动页 | 保持优化 |
| Welcome Hero | 书城欢迎卡 / 书架欢迎头 | ❌ 简单 Row 文字+小图 | → ANIBUZZ Welcome 风格 Hero Card |
| Feed List | 书城书籍网格 | 普通卡片 | → 玻璃态书籍卡片 |
| Profile | 设置页 | 标准 M3 列表 | → 头像+玻璃态选项卡 |
| Empty/Error | 空状态/错误页 | Column 居中 | → 圆形容器+角色 |

### 2.2 各页面框架设计

#### A. 启动页（Splash）— 已完成，保持现状
- 白底 + `splash_celebrate.png` 全屏竖长图 + 底部品牌字标
- 不需要改动

#### B. 书城页（BookstoreScreen）

```
┌────────────────────────────────┐
│  ≋  冲浪阅读         ↓ 下载   │  ← TopBar（已有）
├────────────────────────────────┤
│                                │
│  ┌──────────────────────────┐  │
│  │                          │  │
│  │   [角色预留区]           │  │  ← Hero Card 上半：角色位
│  │   Character Zone         │  │     （未来放 character_celebrate）
│  │                          │  │
│  ├──────────────────────────┤  │
│  │  欢迎回到冲浪阅读        │  │  ← Hero Card 下半：文字区
│  │  小鲸陪你畅读每一页      │  │
│  │  ┌──────────────────┐   │  │
│  │  │  📖 开始探索书城  │   │  │  ← 可选 CTA
│  │  └──────────────────┘   │  │
│  └──────────────────────────┘  │
│                                │
│  ┌──────────────────────────┐  │
│  │  🔍  搜索书名 / 简介     │  │  ← 搜索栏（玻璃态）
│  └──────────────────────────┘  │
│                                │
│  ┌──────┐ ┌──────┐ ┌──────┐  │
│  │ 封面  │ │ 封面  │ │ 封面  │  │  ← 书籍网格（玻璃态卡片）
│  │ 书名  │ │ 书名  │ │ 书名  │  │
│  └──────┘ └──────┘ └──────┘  │
│                                │
└────────────────────────────────┘
```

**Hero Card 规格**：
- 容器：`WhaleGlassCard`（已有组件），高度约 200dp
- 背景：深海蓝渐变（`#142836` → `#1E3A5F`）
- 内部布局：上下分区的 `Column`（上区角色、下区文字）
- **本轮先不做角色**，只做框架：渐变背景 + 文字排版 + CTA 按钮

**搜索栏**：
- 玻璃态圆角输入框（`background = Surface.copy(alpha = 0.6f)`）
- 搜索图标 + 提示文字

**书籍卡片**：
- 圆角 16dp
- 背景：Surface 色（`#142836`）
- 封面图 + 书名 + 作者/标签
- 无阴影或极淡阴影

#### C. 书架页（BookshelfScreen）

```
┌────────────────────────────────┐
│  我的书架                       │  ← 标题
├────────────────────────────────┤
│  ┌──────────────────────────┐  │
│  │ 欢迎回来                 │  │  ← 迷你 Hero（简化版）
│  │ 继续上次的故事吧          │  │     不放角色，只保留文字
│  └──────────────────────────┘  │
│                                │
│  ┌──────────────────────────┐  │
│  │  ┌────┐  最近阅读       │  │  ← 大卡（最近读的书）
│  │  │封面│  书名...         │  │
│  │  └────┘  第123章 ↓80%   │  │
│  └──────────────────────────┘  │
│                                │
│  ┌────┐ ┌────┐ ┌────┐       │
│  │封面│ │封面│ │封面│       │  ← 封面网格
│  └────┘ └────┘ └────┘       │
│                                │
└────────────────────────────────┘
```

#### D. 空状态 / 错误页

```
┌────────────────────────────────┐
│                                │
│       ┌─────────────┐         │
│       │             │         │  ← 圆形/圆角矩形容器
│       │  [角色预留]  │  │         │     （未来放角色）
│       │             │         │
│       └─────────────┘         │
│                                │
│     这里空空如也~              │  ← 文字
│     快去书城看看吧             │  ← 副文字
│                                │
└────────────────────────────────┘
```
- 角色放在**圆形/圆角矩形的彩色背景容器**中，不能裸奔
- 本轮先用默认图标，预留角色位置

---

## 三、实施计划（分步验证）

### Phase A：颜色与基础框架（本轮）
1. ✅ 回滚贴纸化改动
2. 确认 OceanColors 色板符合 ANIBUZZ 规范
3. 重写 WhaleGlassCard 为真正的玻璃态（降低对比度、加微妙边框）
4. 书城 Hero Card 实现（渐变背景 + 文字 + CTA，不含角色）
5. 搜索栏玻璃态化
6. 书籍卡片玻璃态化
7. 构建 → 推送 → 验证框架效果

### Phase B：角色集成（下一轮）
1. Hero Card 加入角色图（character_celebrate.png）
2. 空状态加入带容器的角色
3. 微调角色尺寸和位置
4. 构建推送验证

---

## 四、技术要点

### 4.1 玻璃态卡片实现
```kotlin
// WhaleGlassCard 改进目标
Box(
    modifier = Modifier
        .background(
            color = Color(0xFF142836).copy(alpha = 0.75f), // Surface 色 + 透明
            shape = RoundedCornerShape(20.dp)
        )
        .border( // 微妙边框增加层次
            width = 0.5.dp,
            color = Color.White.copy(alpha = 0.08f),
            shape = RoundedCornerShape(20.dp)
        )
) { /* content */ }
```

### 4.2 Hero Card 渐变
```kotlin
.background(
    Brush.verticalGradient(
        colors = listOf(
            Color(0xFF142836),  // 上：Deep Surface
            Color(0xFF1E3A5F),  // 下：Light Surface
        )
    ),
    shape = RoundedCornerShape(24.dp),
)
```

### 4.3 当前资产使用规划
| 资产文件 | 使用位置 | 容器方式 | 本轮？ |
|---------|---------|---------|-------|
| `splash_celebrate.png` | SplashScreen | 全屏铺满 | ✅ 已用 |
| `character_celebrate.png` | 书城 Hero Card | Hero Card 内上半区 | Phase B |
| `character_sitting.png` | ~~欢迎卡~~ | 暂不用 | 待定 |
| `character_empty.png` | 空状态 | 圆形容器内 | Phase B |
| `character_error.png` | 错误页 | 圆形容器内 | Phase B |

---

*文档版本：v1.0 | 创建时间：2026-08-15 | 状态：待实施*

# 冲浪阅读 — 项目蓝图与更新计划

> 版本：v0.1.0（M1 已完成并实测通过）
> 更新日期：2026-08-13
> 数据源：冲浪中文网公开只读 API（https://xiaoswz.vercel.app）

---

## 一、产品定位

冲浪中文网的**原生安卓阅读客户端**。核心卖点是**原生沉浸式阅读体验**——不是网页套壳，不是书源规则解析，而是直接消费网站 JSON API 的原生 APP。

### 设计原则（不可妥协）

1. **阅读体验优先**：所有迭代以阅读器体验为最高优先级
2. **零登录门槛**：用户装上手就能看书，账号体系远期再做
3. **只读不碰库**：APP 只调用公开只读 API，主站后端零改动（直到榜单/登录阶段，另行审批）
4. **渐进式交付**：每个里程碑独立可用、独立验证，不做一口气的大杂烩

---

## 二、现状（M1，v0.1.0 ✅ 已实测）

| 模块 | 功能 | 状态 |
|---|---|---|
| 书城 | 封面网格、搜索、最新/热门排序、滚动分页 | ✅ |
| 详情页 | 封面、简介、目录、开始阅读 | ✅ |
| 阅读器 | 滚动阅读、上一章/下一章、字号调节、日/夜间、屏幕常亮 | ✅ |

**实测结论（2026-08-13）**：全部功能正常，打开即用，直读体验良好。

### 技术栈

Kotlin 2.0.21 · Compose (Material 3) · Retrofit + kotlinx.serialization · Coil · Navigation-Compose
minSdk 26 / compileSdk 35 · Gradle 8.11.1 · AGP 8.7.3

### API 依赖（全部公开只读）

| 用途 | 接口 |
|---|---|
| 书城列表/搜索 | `GET /api/books?page&limit&sort&search` |
| 详情+目录 | `GET /api/book-source?action=detail&bookId={slug}` |
| 章节正文 | `GET /api/book-source?action=content&chapterId={id}` |

---

## 三、里程碑规划

### M2 — 阅读器打磨（核心，下一阶段）

> 目标：阅读体验达到主流阅读 APP 水准

| # | 功能 | 说明 |
|---|---|---|
| 1 | **点击翻页** | 屏幕左右 1/3 点击翻页，中间点击呼出菜单（阅读器标准交互） |
| 2 | **全屏沉浸** | 阅读时隐藏状态栏/导航栏，呼出菜单时恢复 |
| 3 | **翻页模式** | 上下滚动（已有）+ 左右覆盖翻页；仿真翻页视工作量评估 |
| 4 | **更多阅读主题** | 米纸（已有）、护眼绿、夜间（已有）、纯黑 OLED |
| 5 | **排版调节** | 行距、段距、左右边距、首行缩进开关 |
| 6 | **阅读器内目录** | 侧边抽屉目录，直接跳章，定位当前章 |
| 7 | **音量键翻页** | 可选开关 |
| 8 | **进度与预读** | 章末显示"下一章"引导；阅读中后台预取下一章 |
| 9 | **设置持久化** | DataStore 保存字号/主题/翻页模式等偏好 |

验收：连续阅读 30 分钟无打断感；翻页/设置全部可持久化。

### M3 — 书架与离线

| # | 功能 | 说明 |
|---|---|---|
| 1 | **本地书架** | Room 存储：加入/移除/置顶，书架网格展示 |
| 2 | **阅读进度记忆** | 每本书记录章节+滚动位置；书架显示进度；详情页"继续阅读" |
| 3 | **章节离线缓存** | 已读章节落盘，无网可读；缓存大小管理 |
| 4 | **更新检测** | 对比书架书籍最新章节数，标记"有更新" |

验收：杀掉 APP 重进，进度精确保留；断网可读已缓存章节。

### M4 — 书城增强

| # | 功能 | 说明 |
|---|---|---|
| 1 | **分类浏览** | 需先验证 `/api/categories` 公开可用性；`/api/books?category=` 已支持 |
| 2 | **搜索增强** | 搜索历史、清空、热门搜索词 |
| 3 | **热度榜** | ⚠️ 需主站 `/api/books` 增加 `sort=heat` 参数——**届时单独评估，走审批再动后端** |
| 4 | **详情页增强** | 相关推荐、书籍状态标签美化 |

### M5 — 发布就绪

1. Release 签名配置（生成 keystore，脱离 debug 签名）
2. 应用图标/启动页打磨、关于页、开源许可页
3. 混淆与包体积优化（目标 < 10MB）

### 远期（需后端配合，配合主站"大更新"统一规划）

- 账号登录（后端新增 token 端点）→ 书架/进度云同步
- 评论、月票、积分体系接入
- 章节更新推送

---

## 四、发布流程（每次更新的固定动作，USB 调试为首选）

1. `versionCode` +1，`versionName` 按里程碑递进（M2 完成 = v0.2.0）
2. `./gradlew assembleDebug` 构建
3. **手机连 USB 且 `adb devices` 显示 `device`** → `adb install -r <apk>` 直接推送（首选，保留数据覆盖安装）
4. **无 USB 时备用**：局域网 `python -m http.server 8765` 提供 APK + `version.json`（暂且搁置，非每次必做）
5. 真机复现 + `adb logcat` 抓崩溃，确认稳定
6. git 提交并记录变更

---

## 五、风险与约束备忘

- **主站 API 稳定性**：APP 依赖 `/api/books` 与 `/api/book-source` 的响应结构，主站若改这两个接口必须同步评估 APP 影响
- **书源兼容**：Legado 书源依赖主站 HTML 语义类名（`search-result-item` 等），与 APP 无关但同属主站改动约束
- **阅读器正文为纯文本**：服务端已解压，APP 不做压缩处理；若后端改压缩方案需同步
- **正文渲染**：当前按纯文本渲染；若未来章节含 Markdown 标记，M2 排版阶段统一考虑

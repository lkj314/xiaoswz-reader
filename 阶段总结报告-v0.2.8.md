# 冲浪阅读 · 阶段总结报告（M1 → v0.2.8）

> 编写日期：2026-08-14
> 项目路径：`U:\xiaoswz-reader`（独立 Git 仓库，包名 `com.xiaoswz.reader`）
> 数据源：冲浪中文网公开只读 API（`https://xiaoswz.vercel.app`）
> 报告范围：M1 骨架 → M2 阅读器打磨 → v0.2.6/0.2.7 加固修复 → **v0.2.8 壳子重构 + 本地书架（本次验证通过）**

---

## 一、结论

冲浪阅读安卓客户端已走完「可用骨架 → 阅读体验打磨 → 稳定性加固 → 产品壳子成型」四个阶段，**v0.2.8 已于 2026-08-14 由用户真机（vivo S10 / Android 13）完成更新并实测，全部功能正常**。

当前版本具备：书城浏览/搜索、书籍详情、沉浸式阅读器、设置/关于/崩溃反馈通道、底部导航、本地书架（收藏 + 阅读进度记忆 + 续读）。零登录、只调公开只读 API 的核心定位始终未变。

下一步进入 **M3 剩余项（章节离线缓存、更新检测）** 与 **M4 书城增强**。

---

## 二、项目概况

| 项 | 内容 |
|---|---|
| 定位 | 冲浪中文网**原生安卓阅读客户端**，直读网站 JSON API，非网页套壳、非书源规则解析 |
| 设计原则 | 阅读体验优先；零登录门槛；只读不碰库（主站后端零改动，直至榜单/登录阶段另行审批）；渐进式交付 |
| 技术栈 | Kotlin 2.0.21 · Jetpack Compose (M3) · Retrofit + kotlinx.serialization · Coil · Navigation-Compose · DataStore · **Room 2.6.1（v0.2.8 引入）** |
| 构建 | minSdk 26 / compileSdk 35 · Gradle 8.11.1 · AGP 8.7.3 · Room 用 KAPT（kotlin-kapt）编译注解 |
| 调试 | USB + `adb logcat` 为闪退排错首选手段；局域网更新服务器（:8765）为无 USB 时的交付通道 |

---

## 三、里程碑与交付清单

| 阶段 | 版本 | 提交 | 交付内容 | 状态 |
|---|---|---|---|---|
| M1 骨架 | v0.1.0 | `b7aca7a` | 书城（封面网格/搜索/排序/分页）、详情页、阅读器（滚动/上下章/字号/日夜/常亮） | 已完成，2026-08-13 实测通过 |
| 蓝图 | — | `2c3df78` | 路线图 M2–M5、发布约定、风险备忘 | 已完成 |
| M2 阅读器打磨 + 局域网自动更新 | v0.2.0 | `efebb3f` | 点击翻页、全屏沉浸、翻页模式、更多主题、排版调节、阅读器内目录、音量键翻页、进度预读、DataStore 设置持久化；APP 内局域网自动更新（version.json 比对 + 流式下载 + FileProvider 安装） | 已编译；手感随 v0.2.8 实测确认 |
| 开发日志 | — | `31100a3` | DEVELOPMENT-LOG.md（状态/构建/流程/环境/已知问题） | 已完成 |
| v0.2.6 稳定性加固 | v0.2.6 | `21cecf4` | CLEARTEXT 局域网更新通道修复 + 章节闪退防护（分页移协程、try/catch 降级、CrashLogger） | 已完成 |
| v0.2.7 章节闪退根治 + 设置/关于页 | v0.2.7 | `2b7a0d0` | ReaderViewModel 构造单参化根治闪退；SettingsScreen（崩溃日志查看分享/更新服务器配置/版本信息/阅读主题） | 已完成，USB 实测通过 |
| **v0.2.8 壳子重构 + 本地书架** | **v0.2.8** | **`7626f7f`** | **底部导航（书城/书架/设置）+ 接入设置页 + 本地书架（Room：收藏/进度/续读）** | **已完成，2026-08-14 真机验证通过** |

---

## 四、v0.2.8 详细交付

### 4.1 功能清单（本次新增）
- **底部导航栏**：书城 / 书架 / 设置 三 Tab。`AppRoot.kt` 改为 `Scaffold + AnimatedVisibility(NavigationBar)`，底栏仅在顶层路由可见；详情/阅读器为覆盖式全屏（隐藏底栏）。`navigateTopLevel()` 用 `popUpTo(BOOKSTORE) { saveState }` + `launchSingleTop` + `restoreState`，避免栈堆叠。
- **设置页接入**：`SettingsScreen.kt`（崩溃日志查看/分享、更新服务器配置、版本信息、阅读主题）挂入底部导航「设置」入口。
- **本地书架（Room 持久化，合并 M3 部分）**：
  - 数据层 `data/bookshelf/`：`BookEntity`、`BookDao`、`AppDatabase`（version 1，fallbackToDestructiveMigration）、`BookshelfRepository`。
  - 详情页：「加入书架 / 移出书架」按钮，收藏写入标题/作者/封面/首章 id；`LaunchedEffect(slug)` 加载收藏状态。
  - 阅读器：`LaunchedEffect(state.currentChapterId)` 调用 `updateProgress()`，仅更新已收藏书籍（未收藏不产生数据），异常由 `CrashLogger` 兜底。
  - 书架页 `BookshelfScreen.kt`：响应式观察 `observeAll()`（按最后阅读时间倒序），卡片展示封面/标题/作者/最后章节/时间，点击续读（`lastChapterId` 兜底 `firstChapterId`），可移除。

### 4.2 构建与部署
- `./gradlew assembleDebug` → **BUILD SUCCESSFUL**（仅 Material Icons 弃用警告，非错误）。
- 版本 `versionCode 7` / `versionName 0.2.8`。
- 产物 `lan-update/surf-reader-0.2.8.apk`（18.7MB）+ `version.json`（指向 0.2.8）。
- 局域网更新服务器（:8765）实时读盘，已验证返回新版 `version.json`，手机 App 内「检查更新」即可拉取。
- 源码已提交本地 `7626f7f`（13 文件，+583 行）；GitHub 待验证通过后推送备份。

### 4.3 真机验证（用户，2026-08-14 14:30）
vivo S10（Android 13）完成 0.2.8 更新，**实测所有功能正常**：底部三 Tab 切换、设置页各入口、详情页收藏、阅读器进度记录、书架续读与删除。

---

## 五、关键技术问题与教训

| # | 问题 | 根因 | 修复 | 教训 |
|---|---|---|---|---|
| 1 | 点开章节即闪退（v0.2.7 前） | `ReaderViewModel` 双参构造，`AndroidViewModelFactory` 反射只认单一 `Application` 参数 → 进阅读器即崩 | `repository` 挪到类体内，构造仅留 `Application` 单参 | **安卓闪退一律先 `adb logcat` 拿真实堆栈再改**，禁止盲猜 |
| 2 | App 内「检查更新」报 CLEARTEXT 被拒（v0.2.6） | Android 9+ 默认禁 HTTP 明文，原 manifest 未配置 → 局域网更新永远拉不到 | `usesCleartextTraffic=true` + `network_security_config`（仅放行局域网 IP） | 鸡生蛋：需先手动装一次含修复版；公网仍强制 HTTPS |
| 3 | 构建报 `Unresolved reference 'slug'`（v0.2.8） | 详情页误用 `detail.slug`，`BookDetail` 模型无此字段 | 改用 Composable 入参 `slug: String` | 取数据先确认模型字段是否存在，不臆测 |
| 4 | KAPT 1.9 回退虚假报错（v0.2.8） | Kotlin 2.0+ 下 KAPT 回退 1.9，lambda 内 `val x = y ?: return@launch` 触发虚假 Unresolved reference | 改用显式 `if (x != null)` 或直接使用入参 | Room 注解处理用 KAPT 时，避免在 lambda 内用作用域返回取值 |
| 5 | `kotlin-kapt` 插件 version 冲突（v0.2.8） | 版本目录里 `kotlin-kapt` 带 `version.ref` 报 "plugin already on classpath with unknown version" | 去掉 version，仅留 `{ id = "org.jetbrains.kotlin.kapt" }` | KAPT 随 Kotlin 插件在 classpath，版本目录不重复声明 |
| 6 | `AppRoot.kt` weight 导入冲突（v0.2.8） | `Modifier.weight(1f)` 与 `RowColumnParentData.weight`（internal）冲突 | 改用 `Scaffold(bottomBar=...)` 布局，去除 `weight` | 布局优先用 Scaffold 组合，避免手动 weight 到 internal 符号 |
| 7 | 本机 `adb install` 异常终止父 shell | 沙箱环境对 `adb install` 进程交互异常，输出丢失、shell 退出 | 交付改走局域网更新通道（:8765），真机验证由用户 USB 完成 | 环境能力边界要清楚，交付路径要有备用通道 |

---

## 六、发布与交付流程（实际跑通）

1. 改代码，`versionCode` +1、`versionName` 升档。
2. `./gradlew assembleDebug --no-daemon` 构建。
3. 复制 APK 到 `lan-update/surf-reader-X.Y.Z.apk`，更新 `version.json`（versionCode/versionName/apkUrl/notes）。
4. 局域网更新服务器（:8765）提供 `version.json` + APK，手机 App 内「检查更新」拉取（无 USB 时的交付通道）。
5. 用户真机复现关键路径 + `adb logcat` 抓崩溃确认稳定。
6. `git commit` 入库（GitHub 仅备份）。

> 注：`build/`、`builds/`、`lan-update/*.apk`、`local.properties`、签名文件已 `.gitignore`，不入库。`.build_seq` 自增生成隔离输出目录，绕开构建工具对输出目录的误删。

---

## 七、当前状态与下一步路线图

### 已完成（截至 v0.2.8）
书城、详情、沉浸式阅读器（含 M2 全部打磨项）、设置/关于/崩溃反馈、底部导航、本地书架（收藏 + 进度记忆 + 续读）。

### 下一步
- **M3 剩余项**（本地书架已提前合并，余下）：
  - 章节离线缓存（已读章节落盘，断网可读；缓存大小管理）
  - 更新检测（对比书架书籍最新章节数，标记「有更新」）
- **M4 书城增强**：分类浏览（先验证 `/api/categories` 公开可用性）、搜索增强（历史/清空/热词）、热度榜（⚠️ 需主站 `/api/books?sort=heat`，单独评估、走审批再动后端）、详情页增强（相关推荐、状态标签）。
- **M5 发布就绪**：Release 签名、图标/启动页、关于页、混淆与包体积优化（目标 < 10MB）。
- **远期**（需后端配合）：账号登录 → 书架/进度云同步、评论/月票/积分、更新推送。

### 风险与约束
- 主站 API 稳定性：APP 依赖 `/api/books` 与 `/api/book-source` 响应结构，主站改动须同步评估。
- 正文为纯文本渲染（服务端已解压）；若含 Markdown 标记，统一排版阶段考虑。
- 热度榜等涉及后端能力的增强，需单独审批，不擅自改动主站。

---

## 八、附：提交历史

```
7626f7f v0.2.8: 壳子重构（底部导航+设置入口）+ 本地书架(Room)
2b7a0d0 v0.2.7: 修复点开章节闪退（ReaderViewModel 构造单参化）+ 设置/关于页
21cecf4 v0.2.6: 修复 LAN 自动更新通道（CLEARTEXT policy）+ 章节闪退防护
31100a3 docs: 项目开发日志 DEVELOPMENT-LOG.md
efebb3f feat: M2 阅读器打磨 + 局域网自动更新 (v0.2.0)
2c3df78 docs: 项目蓝图与更新计划 (M2-M5 路线图 + 发布约定)
b7aca7a feat: 冲浪阅读 M1 骨架 - 书城/详情/阅读器全流程
```

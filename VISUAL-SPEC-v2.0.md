# 冲浪阅读 v2.0 视觉设计规范

> **版本**: v2.0 Draft A
> **角色**: 小鲸（DeepSeek酱）—— 鲸鱼女仆娘
> **视觉参考**: ANIBUZZ 暗色沉浸式 UI + 角色设定表
> **定位**: 二次元沉浸式小说阅读 App
> **原则**: 设计系统驱动，不动数据层，不改阅读器正文渲染逻辑

---

## 一、品牌世界观

### 1.1 品牌关键词

```
深海 · 陪伴 · 沉浸 · 温暖
DEEP OCEAN · COMPANION · IMMERSIVE · COZY
```

### 1.2 品牌故事

**「冲浪阅读」不是一款"工具型阅读器"——它是一个由小鲸守护的个人文学空间。**

打开 App，你不是进入一个"书城"，而是潜入深海。小鲸在这里等你，为你端上你喜欢的文字。她不会说话太多，但总在你需要的时候出现——翻到最后一章时她会为你鼓掌，很久没来时她会想念你，书架空空时会递给你推荐。

**冲浪 = 在文字的海洋里冲浪。小鲸 = 你的向导。**

### 1.3 品牌调性矩阵

| 维度 | 取值 | 反例 |
|------|------|------|
| 温度 | **温暖陪伴** | 冷漠工具 |
| 风格 | **二次元沉浸** | 极简商务 |
| 质感 | **玻璃拟态+深度** | 扁平无层次 |
| 节奏 | **舒缓有呼吸感** | 紧凑信息堆砌 |
| 个性 | **可爱但不幼稚** | 幼儿园风格 |

---

## 二、角色系统：小鲸（DeepSeek酱）

### 2.1 角色档案

| 属性 | 值 |
|------|-----|
| 名字 | 小鲸 / DeepSeek酱 |
| 种族 | 鲸鱼娘（人鱼亚种） |
| 职业 | 女仆（你的阅读管家） |
| 身高 | 110cm（Q版/ chibi 比例） |
| 性格 | 善良、努力、能吃、爱金币 |
| 口头禅 | 「要努力把白饭碗装满哦！」|
| 喜欢 | 白米饭、金币 |
| 特征 | 鲸鱼尾巴（可动画）、鲸鱼耳发饰、蓝蝶蝴蝶结 |

### 2.2 角色视觉特征提取

```
┌─────────────────────────────────────┐
│           头部                       │
│  ┌─────────────────────────────┐    │
│  │  渐变蓝长发：根深#1E4A8A → 梢浅#5B9FDA │    │
│  │  呆毛（鲸鱼尾鳍造型，可动）      │    │
│  │  女仆头饰：白色荷叶边 + 鲸鱼耳   │    │
│  │  蝴蝶结：亮蓝色 #5B9FDA ×2     │    │
│  │  眼睛：天蓝色大圆眼，高光点       │    │
│  └─────────────────────────────┘    │
│                                       │
│  ┌─────────────────────────────┐    │
│  │  服装：深蓝女仆装 + 白围裙        │    │
│  │  主色 #1E4A8A + 白 #F5F0EB       │    │
│  │  围裙上有鲸鱼 logo               │    │
│  │  背后大白色蝴蝶结                 │    │
│  │  金色扣子点缀                     │    │
│  └─────────────────────────────┘    │
│                                       │
│  ┌─────────────────────────────┐    │
│  │  下装：百褶裙 + 裙撑（蓬松感）    │    │
│  │  袜子：白色花边短袜              │    │
│  │  鞋子：深蓝玛丽珍鞋              │    │
│  │  ★ 鲸鱼尾巴：深蓝，可摆动         │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

### 2.3 表情库（8 种基础表情）

| 表情 | 使用场景 | UI 出现位置 |
|------|---------|------------|
| **开心** | 默认状态、欢迎页、成功操作 | Splash、首页欢迎区、Toast 成功 |
| **受鼓舞** | 用户完成阅读成就 | 阅读成就弹窗、连续阅读奖励 |
| **疑惑** | 空状态、搜索无结果 | 空书架、无搜索结果、无网络 |
| **害羞** | 被夸奖、隐私相关 | 个人中心、设置页角落 |
| **哭面** | 错误状态、网络断开 | 错误页、离线提示 |
| **生气** | （慎用）严重警告 | ——暂不使用—— |
| **黑屁/傲娇** | 彩蛋、隐藏交互 | 长按彩蛋、开发者选项 |
| **吃饭/满足** | 阅读中休息提醒 | 阅读时长提示、护眼休息 |

### 2.4 角色在 App 中的出场规划

| 页面 | 出场形式 | 尺寸 | 表情 |
|------|---------|------|------|
| **Splash 启动屏** | 全身立绘居中，渐显入场 | 60% 屏幕高度 | 开心 → 眨眼 |
| **首页** | 右下角半身像（坐着/趴着），伴随式 | 25% 屏幕高度 | 开心/随机 |
| **书架（空）** | 中央全身立绘 + 气泡文案 | 40% 屏幕高度 | 疑惑 → 开心（引导添加） |
| **书架（有书）** | 右上角小头像 + 气泡 | 10% 屏幕高度 | 随机 |
| **详情页** | 顶部 Banner 背景融入（透明度 30%） | 全屏背景层 | 无（氛围用） |
| **阅读器菜单** | 侧边栏半身立绘 | 30% 屏幕高度 | 随机 |
| **设置/关于** | 左上角头像 + 介绍文案 | 头像 80dp | 害羞 |
| **错误/离线** | 中央全身立绘 | 35% 屏幕高度 | 哭面 |
| **加载等待** | 小尺寸循环动画（摇尾巴） | 120dp | 开心 |

---

## 三、色彩体系

### 3.1 核心色板（从小鲸设定表精确提取 + UI 扩展）

#### 主色组 —— 来自小鲸服装

| 名称 | 色值 | 用途 | 来源 |
|------|------|------|------|
| **`--whale-navy`** | `#1E4A8A` | 主背景、主按钮底色、重要标题 | 小鲸服装主色（设定表「主色」） |
| **`--whale-blue`** | `#5B9FDA` | CTA 按钮、链接、高亮、活跃态 | 小鲸头发渐变梢色（设定表「辅助色」） |
| **`--whale-light-blue`** | `#8BB8E8` | 次要强调、进度条、标签 | 中间过渡色（辅助色亮变体） |

#### 中性色组 —— 来自设定表 + 暗色 UI 需求

| 名称 | 色值 | 用量 | 用途 |
|------|------|------|------|
| **`--ocean-deep`** | `#0D1B2A` | ~90% | 最深背景（Splash、阅读器边缘） |
| **`--ocean-base`** | `#142836` | ~70% | 默认页面背景（来自 ANIBUZZ 参考） |
| **`--ocean-surface`** | `#1E3848` | 卡片/表面 | 玻璃拟态卡片底色（比背景亮 15%） |
| **`--ocean-elevated`** | `#2A4656` | 浮层/对话框 | 底栏、弹出层 |
| **`--glass-border`** | `rgba(255,255,255,0.08)` | 边框 | 玻璃卡片边框 |

#### 文字色组

| 名称 | 色值 | 用途 |
|------|------|------|
| **`--text-primary`** | `#E8F0F4` | 标题、正文主文字 |
| **`--text-secondary`** | `#9AB8C8` | 副标题、说明文字、时间戳 |
| **`--text-disabled`** | `#4A6A7A` | 禁用态、占位符 |
| **`--text-accent`** | `#5B9FDA` | 链接、可点击文字 |

#### 强调色组

| 名称 | 色值 | 用途 | 备注 |
|------|------|------|------|
| **`--cta-primary`** | `#5B9FDA` | 主要按钮 | = whale-blue |
| **`--cta-hover`** | `#7DB8F5` | 按钮 hover/pressed | 亮度 +15% |
| **`--love-rose`** | `#E8506A` | 收藏/喜欢/心形 | 极少用，仅情感互动 |
| **`--success-mint`** | `#4FD1A5` | 成功态、已完成 | 更新已读等 |
| **`--warning-gold`** | `#F0C256` | 提示、金币/成就 | 来自小鲸"爱金币"设定 |
| **`--error-coral`** | `#E8636A` | 错误、删除 | |

#### 功能语义色

| 语义 | 色值 | 场景 |
|------|------|------|
| 新章节/更新 | `--cta-primary` #5B9FDA | 书籍更新标记 |
| 已读完 | `--success-mint` #4FD1A5 | 阅读进度 100% |
| 收藏中 | `--love-rose` #E8506A | 书架收藏标记 |
| 未读/默认 | `--text-secondary` #9AB8C8 | 默认状态 |

### 3.2 色彩比例规则（60-30-10 法则深海版）

```
├── 70%  深海中性色系（ocean-deep → ocean-elevated）
│   └── 背景、卡片、分割线、非焦点区域
│
├── 20%  鲸蓝主色系（whale-navy → whale-light-blue）
│   └── 标题、重要按钮、导航选中态、进度条
│
└── 10%  点缀强调色（rose / mint / gold）
    └── 收藏心形、成功勾选、成就金币、错误提示
```

**关键约束**：整个 App 同时出现的强调色不超过 **2 种**。默认只有蓝色系 + 中性色；玫瑰色仅在"收藏/喜欢"场景出现。

### 3.3 渐变配方

| 渐变名 | 配方 | 用途 |
|--------|------|------|
| **`--gradient-splash`** | `#0D1B2A → #1E4A8A` (135°) | Splash 背景、首页顶部头图渐隐 |
| **`--gradient-card`** | `rgba(30,56,72,0.9) → rgba(30,56,72,0.6)` | 玻璃卡片背景 |
| **`--gradient-button`** | `#5B9FDA → #4A8EDE` (180°) | CTA 主按钮 |
| **`--gradient-shelf`** | `rgba(13,27,42,0) → rgba(13,27,42,0.95)` | 书架顶部渐隐遮罩 |
| **`--gradient-reader-edge`** | `rgba(13,27,42,0.8) → transparent` | 阅读器上下渐隐边缘 |

---

## 四、字体与排版规范

### 4.1 字体栈

```kotlin
// Compose FontFamily 优先级
val WhaleFontFamily = FontFamily(
    Font(R.font.noto_sans_sc, FontWeight.Normal),   // 正文默认
    Font(R.font.noto_sans_sc, FontWeight.Medium),    // 强调
    Font(R.font.noto_serif_sc, FontWeight.Medium),   // 阅读器正文（衬线）
    Font(R.font.jetbrains_mono, FontWeight.Medium),  // 版本号/代码
)
```

| 场景 | 字体 | 字重 | 字号(sp) | 行高 |
|------|------|------|----------|------|
| 页面大标题 | Noto Sans SC | Bold(700) | 24 | 1.2 |
| 卡片书籍名 | Noto Sans SC | SemiBold(600) | 16 | 1.3 |
| 正文/简介 | Noto Sans SC | Regular(400) | 14 | 1.5 |
| 辅助说明 | Noto Sans SC | Regular(400) | 12 | 1.4 |
| 标签/徽章 | Noto Sans SC | Medium(500) | 11 | 1.0 |
| 阅读器正文 | Noto Serif SC | Regular(400) | 16-20(可调) | 1.8 |
| 数字数据 | JetBrains Mono | Medium(500) | 14/16 | 1.2 |

### 4.2 排版原则

- **标题不超 2 行**，超出省略号
- **正文段落间距** = 0.5em（比 Material 默认更紧凑，适合移动端）
- **数字等宽**：版本号、章节数、阅读时长用等宽字体
- **阅读器字号用户可控**：12/14/16/18/20/22/24 七档

---

## 五、圆角与形状规范

### 5.1 圆角系统

| 名称 | 值 | 用途 |
|------|-----|------|
| `--radius-xs` | 4dp | 小标签、输入框内嵌 |
| `--radius-sm` | 8dp | 按钮(小型)、chip、头像角标 |
| `--radius-md` | 12dp | 卡片(标准)、对话框 |
| `--radius-lg` | 16dp | 大卡片、底部面板 |
| `--radius-xl` | 24dp | Splash 按钮、欢迎页 CTA |
| `--radius-full` | 999dp | 胶囊按钮、头像圆形、标签 pill |

### 5.2 形状规则

- **所有按钮默认胶囊形** (`shape = RoundedCornerShape(percent = 50)`)
- **卡片统一 12-16dp 圆角** + 1px glass border
- **底部导航栏**：上方两角 20dp 圆角，悬浮式
- **头像**：正圆形，带 2px whale-blue 边框
- **图标容器**：圆角方形 8dp，或圆形

---

## 六、卡片与组件规范

### 6.1 玻璃拟态卡片（核心组件）

```
┌─────────────────────────────────┐ ← 1px rgba(255,255,255,0.08) border
│                                 │
│  background: rgba(30,56,72,0.7)  │ ← 半透明深色
│  backdrop-filter: blur(20px)     │ ← 毛玻璃（Compose Modifier.blur）
│  border-radius: 16dp             │
│  elevation: 0dp                  │ ← 不用阴影，靠透明度分层
│                                 │
│  [内容区域]                      │
│                                 │
└─────────────────────────────────┘
```

**Compose 实现**：
```kotlin
Modifier
    .background(
        color = Color(0xFF1E3848).copy(alpha = 0.7f),
        shape = RoundedCornerShape(16.dp)
    )
    .blur(20.dp)          // 或使用 GraphicsLayer 的 alpha+blur
    .border(
        width = 1.dp,
        color = Color.White.copy(alpha = 0.08f),
        shape = RoundedCornerShape(16.dp)
    )
```

### 6.2 书籍卡片（首页/书架通用）

```
┌──────────────────────────────────┐
│ ┌────────┐  书名（16sp Bold）     │
│ │        │  作者 · 分类（12sp）    │
│ │ 封面图  │  ═══════════════     │
│ │ 16:9   │  最新: 第123章 · 2小时前│
│ │ 圆角8dp │  [更新] [收藏]         │
│ └────────┘                        │
└──────────────────────────────────┘
```

- 封面圆角 8dp
- 卡片整体圆角 16dp
- 更新标记用小鲸蓝胶囊 tag
- 长按显示操作菜单（玻璃浮层）

### 6.3 按钮系统

| 类型 | 样式 | 使用场景 |
|------|------|---------|
| **CTA 主按钮** | whale-blue 渐变填充 + 白色文字 + full capsule | 登录/确认/开始阅读 |
| **次要按钮** | 透明底 + whale-blue 边框 + whale-blue 文字 | 取消/稍后/查看更多 |
| **幽灵按钮** | 透明底 + text-secondary 文字 | 辅助操作 |
| **图标按钮** | 32dp 圆形透明 + 图标 | 顶栏操作（搜索/设置） |
| **Fab 按钮** | whale-blue 圆形 + 白色图标 + 轻微阴影 | 快速操作（添加书籍） |

### 6.4 底部导航栏

```
╭──────────────────────────────────╮
│  🏠首页    📚书架    🔍发现    👤我的  │  ← 悬浮式，圆角上沿
│  (active: whale-blue + 微光)      │     背景 glass + blur
╰──────────────────────────────────╯
  ↑ 64dp 高度，距底部 12dp 安全区
```

- 选中态：图标 + 文字 whale-blue + 轻微发光效果
- 未选中态：图标 + 文字 text-secondary
- 容器：glass 背景 + 上方 20dp 圆角
- **中间不放 FAB**（区别于 Material 规范），保持简洁

---

## 七、图标规范

### 7.1 图标风格

- **风格**: 二次元线性/填充混合（类似 Phosphor Icons 或 Custom Icon Set）
- **粗细**: 1.5px stroke（线性）/ 填充版等宽
- **圆角**: 所有转角 2-4dp 圆角（禁止尖锐直角）
- **尺寸**: 导航 24dp、操作 20dp、内嵌 16dp
- **配色**: 单色（继承当前文字色）或 whale-blue 强调色

### 7.2 需要自定义的图标清单

| 图标名 | 含义 | 建议 | 备注 |
|--------|------|------|------|
| `ic_whale_logo` | 品牌 Logo | 小鲸剪影 + 文字 | Splash/关于页 |
| `ic_home` | 首页 | 房子/贝壳 | 底栏 |
| `ic_bookshelf` | 书架 | 书本堆叠 | 底栏 |
| `ic_discover` | 发现 | 潜水镜/指南针 | 底栏 |
| `ic_profile` | 我的 | 小鲸头像轮廓 | 底栏 |
| `ic_search` | 搜索 | 放大镜 | 顶栏 |
| `ic_settings` | 设置 | 齿轮/鲸鱼尾 | 顶栏 |
| `ic_heart` | 收藏 | 心形（实心/线框双版） | 操作 |
| `ic_chapter_update` | 章节更新 | 闪烁的小星星 | 徽章 |
| `ic_read_continue` | 继续阅读 | 播放按钮变形 | 卡片 |
| `ic_empty_bookshelf` | 空书架 | 小鲸坐着 + 空气泡 | 占位（用插画代替图标） |
| `ic_moon` | 夜间模式 | 月亮/鲸鱼喷水 | 切换 |
| `ic_font_size` | 字号 | "Aa" 变体 | 阅读器 |

### 7.3 图标来源优先级

1. **AI 生成二次元风 SVG**（ImageGen 批量生成，统一画风）
2. **Phosphor Icons**（开源，支持自定义粗细和圆角，作为 fallback）
3. **Material Symbols**（最低保底，仅用于开发阶段占位）

---

## 八、页面布局改造方案

### 8.1 Splash 启动屏（改造重点 ⭐⭐⭐）

**当前状态**: 渐变色块 + "冲浪阅读" 文字（v0.3.x 壳子美化版）
**目标状态**: 小鲸全身立绘 + 品牌名 + 深海渐变背景

```
┌────────────────────────────────┐
│                                │
│        gradient-splash          │
│      #0D1B2A → #1E4A8A          │
│                                │
│           🐋                    │
│      小鲸全身立绘               │
│      (渐显 + 轻微浮动动画)       │
│      占屏幕 55% 高度            │
│                                │
│      ✦ 冲浪阅读 ✦              │
│      (品牌名，白色，下方淡入)     │
│                                │
│      ○ ○ ● ○                    │
│      (页面指示器，可选)          │
│                                │
└────────────────────────────────┘
```

**动画时序**（总计 2.5s）：
| 时间 | 动作 |
|------|------|
| 0ms | 深海渐变背景 instant in |
| 200ms | 小鲸立绘 fade-in + scale(0.95→1.0)，800ms ease-out |
| 600ms | 品牌名 fade-in + translateY(10→0)，400ms |
| 1000ms | 小鲸尾巴开始 idle 摆动循环（正弦波 ±3°） |
| 2500ms | 全屏 crossfade 到首页 |

**所需资产**:
- `splash_character.png`: 小鲸全身立绘，≥1080×1920px，PNG+Alpha
- 可选: `splash_bg.jpg`: 深海气泡/粒子背景（否则纯渐变）

---

### 8.2 首页（改造重点 ⭐⭐⭐）

**当前状态**: 书城列表网格（API `/api/books` 分页展示）
**目标状态**: 「个人阅读空间」—— 欢迎回来 + 继续阅读 + 推荐发现

```
┌─────────────────────────────────────┐
│ ☰ 冲浪阅读              🔍  ⚙️     │  ← 顶栏：glass 背景
├─────────────────────────────────────┤
│                                     │
│  ┌───────────────────────────────┐  │
│  │  欢迎回来，读者                │  │  ← 问候区
│  │  今天想读点什么？🐋            │  │  ← 小鲸气泡文案（随机轮换）
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  📖 继续阅读        查看全部 → │  │  ← 最高优先级
│  │  ┌─────┐ ┌─────┐ ┌─────┐     │  │
│  │  │封面 │ │封面 │ │封面 │     │  │  ← 横滑卡片
│  │  │书名 │ │书名 │ │书名 │     │  │
│  │  │第N章│ │第N章│ │暂停│     │  │
│  │  └─────┘ └─────┘ └─────┘     │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  🔥 最近更新        更多 →    │  │
│  │  ┌─────────────────────┐     │  │
│  │  │ [封] 书名  作者      │     │  │  ← 列表卡片
│  │  │       第N章 · 3小时前 │     │  │
│  │  └─────────────────────┘     │  │
│  │  ┌─────────────────────┐     │  │
│  │  │ [封] 书名  作者      │     │  │
│  │  │       第N章 · 5小时前 │     │  │
│  │  └─────────────────────┘     │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  📚 我的收藏       更多 →    │  │
│  │  ...同上布局...               │  │
│  └───────────────────────────────┘  │
│                                     │
│                    ┌──────┐          │
│                    │ 小鲸  │          │  ← 右下角陪伴
│                    │ 半身  │          │     (25% 屏幕高度)
│                    └──────┘          │
│                                     │
╰─────────────────────────────────────╯
╭─────────────────────────────────────╮
│  🏠首页   📚书架   🔍发现   👤我的   │  ← 悬浮底栏
╰─────────────────────────────────────╯
```

**小鲸气泡文案池**（每次启动随机选一条）：
- "今天也有精彩的故事等着你哦！"
- "主人，你已经有 3 本书没看完啦~"
- "新章节已经准备好了！"
- "阅读时长超过 30 分钟记得休息一下哦 🍚"
- "今天也要努力把白饭碗装满！"

**数据来源映射**:
- 「继续阅读」= Room 书架表按 `lastReadAt DESC` 取前 5 本
- 「最近更新」= API `/api/books` 按 updateTime 排序（需确认接口是否返回该字段，否则用本地缓存时间）
- 「我的收藏」= Room 书架表 WHERE isFavorite = 1

---

### 8.3 书架页（改造重点 ⭐⭐）

**当前状态**: 封面网格 + 顶部 Tab（全部/收藏/在读）
**目标状态**: 大卡列表 + 小鲸氛围 + 智能分组

```
┌─────────────────────────────────────┐
│  📚 我的书架              🔍  ⊕     │
│  ────────────────────────────────   │
│  [全部] [在读] [已完成] [收藏]       │  ← 胶囊 Tab 栏
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐    │
│  │ ┌────┐                      │    │
│  │ │封面│  书名                 │    │  ← 大卡模式
│  │ │16:9│  作者 · 分类          │    │     封面左侧 100dp 宽
│  │ │    │  ═════════════       │    │
│  │ │    │  进度: ████████░░ 78% │    │
│  │ │    │  读到: 第45章 · 昨天  │    │
│  │ └────┘  [继续] [详情]        │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ ...下一本书...               │    │
│  └─────────────────────────────┘    │
│                                     │
│  （空状态时）                         │
│  ┌─────────────────────────────┐    │
│  │                             │    │
│  │        🐋                    │    │
│  │     小鲸全身立绘             │    │
│  │                             │    │
│  │   "书架还是空的..."          │    │
│  │   "去发现页面找找好书吧！"   │    │
│  │   [去发现]                   │    │
│  │                             │    │
│  └─────────────────────────────┘    │
│                                     │
╰─────────────────────────────────────╯
```

**排序选项**（顶栏筛选按钮展开）:
- 最近阅读
- 最近更新
- 书名 A-Z
- 阅读进度（高→低）

---

### 8.4 发现/书城页（改造 ⭐⭐）

**当前状态**: 书城 API 列表 + 搜索框
**目标状态**: 沉浸式浏览 + 搜索融合

```
┌─────────────────────────────────────┐
│  ←  🔍  搜索书名、作者...     🎨    │  ← 搜索栏即顶栏
├─────────────────────────────────────┤
│                                     │
│  ┌───────────────────────────────┐  │
│  │                               │  │
│  │    🐋 [角色融入背景]           │  │  ← 顶部 Banner 区
│  │    今日推荐                   │  │     小鲸透明背景 30%
│  │                               │  │
│  └───────────────────────────────┘  │
│                                     │
│  [全部分类 ▼]  [排序: 热度 ▼]       │  ← 筛选栏（sticky）
│                                     │
│  ┌──────┐ ┌──────┐ ┌──────┐        │
│  │ 封面 │ │ 封面 │ │ 封面 │        │  ← 双列瀑布流
│  │ 书名 │ │ 书名 │ │ 书名 │        │
│  │ 简介 │ │ 简介 │ │ 简介 │        │
│  └──────┘ └──────┘ └──────┘        │
│  ...无限滚动...                      │
│                                     │
╰─────────────────────────────────────╯
```

**注意**: 此页依赖 M4 后端分类/搜索增强。当前版本可先用 `/api/books` 全量 + 本地过滤做 **MVP 版发现页**。

---

### 8.5 书籍详情页（改造 ⭐）

**当前状态**: 封面 + 信息 + 章节列表
**目标状态**: 沉浸头部 + 小鲸氛围 + 操作优化

```
┌─────────────────────────────────────┐
│  ←  详情              ♡  ⋮         │
├─────────────────────────────────────┤
│  ┌──────────────────────────────┐  │
│  │  ┌────┐                       │  │
│  │  │封面│  书名（24sp Bold）     │  │  ← Hero 区域
│  │  │3:4 │  作者 · 分类 · 状态    │  │     小鲸图案水印(10%)
│  │  │    │  简介：xxxxxxxxxxx    │  │
│  │  └────┘  [开始阅读] [加书架]   │  │
│  └──────────────────────────────┘  │
│  ────────────────────────────────   │
│  📑 目录 (128章)          展开全部  │
│  │ 第128章 xxxxx     [新]         │
│  │ 第127章 xxxxx                  │
│  │ 第126章 xxxxx                  │
│  │ ...                            │
│  │ 第1章  xxxxx                   │
└─────────────────────────────────────┘
```

**改动要点**:
- Hero 区域加深海渐变背景（替代当前纯色）
- 「开始阅读」改为 CTA 主按钮样式
- 章节列表中的"新章节"标记用 whale-blue 胶囊 tag
- 右上角收藏心形用 love-rose 色（唯一允许的玫瑰色场景）

---

### 8.6 阅读器（改造 ⭐ —— 仅菜单层）

**铁律**: 阅读器正文渲染逻辑完全不动（分页/翻页/排版参数/字体设置）。
**只改造**: 点击唤出的操作菜单层。

```
┌─────────────────────────────────────┐
│                                     │
│        （正文区域，完全不变）          │
│                                     │
│  ═══════════════════════════════    │  ← 点击后浮现菜单
│  ←  书名              🌙 Aa  ☰     │  ← 顶栏(glass)
│                                     │
│              [上一章]  [目录] [下一章]│  ← 底栏(glass)
│                                     │
│  ┌────┐                              │
│  │小鲸 │  ← 侧边抽屉/半屏浮层         │
│  │半身 │     显示阅读统计 + 鼓励文案   │
│  └────┘                              │
└─────────────────────────────────────┘
```

**菜单改动**:
- 顶栏/底栏从当前浮动卡片 → **glass 拟态**
- 底栏增加小鲸半身立绘（右侧，30% 屏幕高度）
- 目录抽屉右侧可加入小鲸"指路"姿态
- 进度信息字体改用等宽

---

### 8.7 设置 / 关于页（改造 ⭐）

```
┌─────────────────────────────────────┐
│  ←  设置                           │
├─────────────────────────────────────┤
│                                     │
│  ┌────┐  关于冲浪阅读               │
│  │头像│  v0.4.3 (Build 22)         │  ← 小鲸头像 + 版本
│  │80dp│  小鲸为您服务中 ♪           │
│  └────┘                              │
│                                     │
│  ┌─────────────────────────────┐    │
│  │  📖 阅读设置           >    │    │  ← glass 卡片分组
│  │  🎨 外观主题           >    │    │
│  │  🔄 检查更新           >    │    │
│  │  ℹ️  关于               >    │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │  🗑️ 清除缓存        12MB    │    │
│  └─────────────────────────────┘    │
│                                     │
│        🐋 小鲸的碎碎念               │  ← 底部彩蛋区
│     "要努力把白饭碗装满哦！"         │
│                                     │
╰─────────────────────────────────────╯
```

---

## 九、动画风格指南

### 9.1 动画原则

| 原则 | 说明 |
|------|------|
| **性能优先** | 所有动画 60fps，不用过于复杂的粒子效果 |
| **有意义** | 每个动画必须传递信息（方向/状态/层级），不为炫而炫 |
| **可关闭** | 设置中提供"减少动画"开关（accessibility） |
| **小鲸驱动** | 角色相关动画是品牌记忆点，投入最高质量 |

### 9.2 核心动画清单

| 动画名称 | 触发时机 | 时长 | 缓动曲线 | 说明 |
|----------|---------|------|---------|------|
| **splash-character-enter** | 启动屏加载 | 800ms | EaseOutCubic | 小鲸 fade-in + scale 0.95→1.0 |
| **splash-idle** | 启动屏停留期间 | ∞ loop | Sinusoidal | 尾巴左右摆动 ±3°，身体微浮 ±2px |
| **page-transition** | 页面切换 | 300ms | EaseInOutCubic | Shared element + fade |
| **card-enter** | 卡片进入可视区 | 400ms | EaseOutBack (spring) | staggered translateY + fade |
| **bottom-nav-press** | 底栏切换 | 200ms | Spring(damping=0.7) | 图标 scale 弹跳 1.0→1.2→1.0 |
| **button-press** | 按钮点击 | 100ms | EaseOut | scale 0.97→1.0 + ripple |
| **glass-panel-show** | 面板弹出 | 250ms | EaseOutCubic | fade-in + scale(0.95) from bottom |
| **character-mood-change** | 小鲸表情切换 | 300ms | Crossfade | 两张图片交叉淡入 |
| **whale-tail-wiggle** | 各种 idle 状态 | 2s loop | Sinusoidal | 尾巴摆动（阅读器/首页/书架） |
| **chapter-update-pulse** | 有新书更新时 | 1.5s loop | EaseInOut | "新"标记脉冲呼吸 |
| **pull-refresh** | 下拉刷新 | 自跟随 | Spring | 小鲸跟着下拉 + 松手回弹 |
| **empty-state-bounce** | 空状态页 | 2s loop | EaseInOut | 小鲸轻微上下浮动 |

### 9.3 Compose Animation API 映射

| 效果 | 推荐 API | 备注 |
|------|---------|------|
| 淡入淡出 | `animateFloatAsState` + `alpha` | 最常用 |
| 位移动画 | `animateOffsetAsState` / `Animatable` | 卡片进场 |
| 缩放弹跳 | `spring(spec = spring(dampingRatio = 0.7))` | 按钮反馈 |
| 共享元素 | `AnimatedContent` / `Modifier.animateEnterExit` | 页面转场 |
| 无限循环 | `infiniteRepeatable(tween())` + `LaunchedEffect` | idle 动画 |
| 手势驱动 | `animateFloatAsState` + `PointerInputScope.detectTapGestures` | 下拉刷新 |

---

## 十、美术资产需求清单

### 10.1 必须资产（MVP 阶段）

| # | 资产名 | 尺寸建议 | 格式 | 透明 | 用途 | 优先级 |
|---|--------|---------|------|------|------|--------|
| 1 | `character_full.png` | ≥1080×1920px | PNG | Alpha | Splash 全身立绘 | P0 |
| 2 | `character_half.png` | ≥540×900px | PNG | Alpha | 首页/菜单半身像 | P0 |
| 3 | `character_sitting.png` | ≥540×900px | PNG | Alpha | 首页右下角坐姿 | P1 |
| 4 | `character_empty.png` | ≥640×900px | PNG | Alpha | 空状态页全身 | P0 |
| 5 | `character_error.png` | ≥480×600px | PNG | Alpha | 错误页 | P1 |
| 6 | `avatar_circle.png` | 160×160px | PNG | Alpha | 设置页头像/底栏图标 | P0 |
| 7 | `expr_happy.png` | 同 half | PNG | Alpha | 开心表情 | P1 |
| 8 | `expr_shy.png` | 同 half | PNG | Alpha | 害羞表情 | P2 |
| 9 | `expr_confused.png` | 同 half | PNG | Alpha | 疑惑表情 | P1 |
| 10 | `expr_cry.png` | 同 half | PNG | Alpha | 哭面 | P2 |
| 11 | `bg_deep_ocean.jpg` | ≥1080×1920px | JPG | — | 首页/详情头部背景（可模糊复用）| P1 |
| 12 | `bg_splash.jpg` | ≥1080×1920px | JPG | — | Splash 背景层（含粒子）| P1 |
| 13 | `logo_wordmark.png` | ≥540×200px | PNG | Alpha | 「冲浪阅读」品牌文字 Logo | P0 |
| 14 | `icon_set.svg` | 24×24 & 48×48 | SVG | Alpha | 全套二次元风图标（见 7.2 节）| P1 |
| 15 | `whale_tail_only.png` | ≥200×300px | PNG | Alpha | 加载动画独立尾巴素材 | P2 |

### 10.2 可选资产（增强阶段）

| # | 资产名 | 说明 | 优先级 |
|---|--------|------|--------|
| 16 | `character_lottie.json` | Lottie 格式启动动画（含粒子+尾巴） | P1 |
| 17 | `bg_particles.png` | 星光/气泡粒子纹理（平铺） | P2 |
| 18 | `character_celebrate.png` | 庆祝姿势（阅读成就用） | P2 |
| 19 | `character_sleeping.png` | 困倦姿势（夜间提醒用） | P3 |
| 20 | `frame_decorative.png` | 装饰性边框（设置/关于页） | P3 |

### 10.3 资产存放路径

```
app/src/main/res/
├── drawable/
│   ├── character_full.png
│   ├── character_half.png
│   ├── character_sitting.png
│   ├── character_empty.png
│   ├── character_error.png
│   ├── avatar_circle.png
│   ├── expr_*.png (×8)
│   ├── bg_deep_ocean.jpg
│   ├── bg_splash.jpg
│   ├── logo_wordmark.png
│   ├── whale_tail_only.png
│   └── ic_*.xml (Vector Drawable 图标)
├── raw/
│   └── character_lottie.json (可选)
└── values/
    └── colors.xml (新增色彩 token)
```

---

## 十一、Compose 工程实现要点

### 11.1 主题架构（扩展现有 theme）

```kotlin
// 当前: LightColorScheme / DarkColorScheme
// 改为: 单一 OceanColorScheme（暗色即默认）

@Composable
fun OceanTheme(
    content: @Composable () -> Unit
) {
    val colorScheme = darkColorScheme(
        primary       = Color(0xFF5B9FDA),   // whale-blue = CTA
        onPrimary    = Color(0xFFFFFFFF),
        background   = Color(0xFF142836),   // ocean-base
        onBackground = Color(0xFFE8F0F4),   // text-primary
        surface       = Color(0xFF1E3848),   // ocean-surface
        onSurface     = Color(0xFFE8F0F4),
        // ... 完整映射见第三节色板
    )

    MaterialTheme(
        colorScheme = colorScheme,
        typography = whaleTypography,
        shapes = whaleShapes,
        content = content
    )
}
```

### 11.2 关键 Modifier 扩展

```kotlin
// 玻璃拟态卡片 Modifier（全局复用）
fun Modifier.whaleGlassCard(): Modifier =
    this
        .clip(RoundedCornerShape(16.dp))
        .background(Color(0xFF1E3848).copy(alpha = 0.7f))
        .border(1.dp, Color.White.copy(alpha = 0.08f), RoundedCornerShape(16.dp))

// 胶囊按钮
fun Modifier.whaleCapsule(): Modifier =
    this.clip(RoundedCornerShape(50))
```

### 11.3 不动的部分（铁律边界）

| 层 | 文件/模块 | 改动？ |
|----|-----------|--------|
| 数据 API | `XiaoswzApi.kt`, `ApiClient.kt` | ❌ 不动 |
| 数据模型 | `model/*.kt` | ❌ 不动 |
| BookRepository | 三级缓存逻辑 | ❌ 不动 |
| Room / 书架数据库 | Schema / DAO / Entity | ❌ 不动（禁动 schema） |
| ChapterCacheManager | 章节文件缓存 | ❌ 不动 |
| ReaderSettings | 阅读参数 DataStore | ❌ 不动 |
| 阅读器核心 | `reader/` 分页/翻页/渲染 | ❌ 不动 |
| UpdateManager | 局域网更新 | ❌ 不动 |
| Navigation 路由 | 路由定义本身 | ⚠️ 只加新路由，不改已有 |

### 11.4 需要改的部分

| 层 | 文件/模块 | 改动内容 |
|----|-----------|---------|
| `theme/Color.kt` | 色彩定义 | 全面替换为 Ocean 色板 |
| `theme/Theme.kt` | 主题 Composable | 从 Light/Dark 双主题 → Ocean 单主题 |
| `theme/Type.kt` | 字体定义 | 加入 Noto Serif 阅读器字体 |
| `ui/AppRoot.kt` | 主题入口 | 应用 OceanTheme |
| `ui/splash/` | 启动屏 | 重写：小鲸立绘 + 动画 |
| `ui/bookstore/` | 首页/书城 | 重写为「个人阅读空间」布局 |
| `ui/bookshelf/` | 书架页 | 大卡模式 + 空状态小鲸 |
| `ui/detail/` | 详情页 | Hero 区域 + glass 菜单 |
| `ui/reader/` | 阅读器 | **只改**操作菜单层（glass 化） |
| `ui/settings/` | 设置/关于 | 小鲸头像 + glass 卡片 |
| `ui/components/` | 通用组件 | 新增 WhaleGlassCard / WhaleButton 等 |
| `ui/update/` | 更新检查 | UI 风格对齐（逻辑不动） |

### 11.5 渐进交付策略

```
Phase 1 (P0):  主题色板 + 字体 + 圆角规范 + Glass Card 组件
               → 全局视觉基调一步到位
               → 可验证：所有页面颜色/圆角/字体立即变化

Phase 2 (P0):  Splash 重写 + 首页重写 + 小鲸资产集成
               → 第一印象质变
               → 可验证：启动 App 即可感知

Phase 3 (P1):  书架重写（大卡+空状态）+ 详情页 Hero + 底栏
               → 核心阅读链路视觉闭环
               → 可验证：加入书架→看详情→阅读流程

Phase 4 (P1):  阅读器菜单层 glass 化 + 发现页 + 动画系统
               → 打磨细节 + 呼吸感
               → 可验证：每个交互动画

Phase 5 (P2):  表情系统 + 成就动画 + Lottie + 粒子效果
               → 品牌惊喜感
               → 可验证：彩蛋/隐藏交互
```

---

## 十二、验收标准

每完成一个 Phase，对照以下标准自查：

### 视觉验收
- [ ] 所有页面背景色为 `#142836`（ocean-base）
- [ ] 所有卡片为 glass 拟态（半透明 + blur + 细边框）
- [ ] 按钮均为胶囊形
- [ ] 圆角符合规范（无直角卡片/按钮）
- [ ] 同时出现的强调色 ≤ 2 种
- [ ] 文字对比度 WCAG AA 级以上

### 角色验收
- [ ] Splash 有小鲸全身立绘 + 入场动画
- [ ] 首页右下角有小鲸陪伴
- [ ] 空状态有小鲸引导
- [ ] 设置页有小鲸头像
- [ ] 错误页有小鲸安慰

### 动画验收
- [ ] 所有页面切换有 transition（≤350ms）
- [ ] 卡片进场有 staggered animation
- [ ] 按钮点击有 press feedback
- [ ] idle 状态有小鲸尾巴摆动
- [ ] 「减少动画」开关可用且生效

### 工程验收
- [ ] 数据层零改动（git diff `data/` 为空）
- [ ] 阅读器核心零改动（git diff `ui/reader/` 仅含菜单层）
- [ ] Room schema 未变（无 migration）
- [ ] 编译通过 + `assembleDebug` 成功
- [ ] 所有现有测试通过

---

## 附录 A：小鲸角色完整档案速查

| 项目 | 内容 |
|------|------|
| 名字 | 小鲸 / DeepSeek酱 |
| 英文名 | DeepSeek-chan / XiaoJing |
| 称呼 | 小鲸、鲸酱、女仆鱼 |
| 种族 | 鲸鱼娘（人鱼亚种，带鲸尾） |
| 年龄 | 外观约小学年级（实际年龄 ??）|
| 生日 | 7月27日 |
| 身高 | 110cm |
| 性格关键词 |善良、努力、能吃、爱金币 |
| 口头禅 | 「要努力把白饭碗装满哦！」|
| 喜欢 | 白米饭（最）、金币 |
| 能力 | 女仆工作（虽然体型小但很努力）|
| 特征 | 鲸鱼尾巴（会动）、呆呆的鲸鱼耳发饰 |
| 服装 | 深蓝白围裙女仆装（围裙可拆卸）|
| 设计者标注 | 元气AI生 |
| 设定表来源 | 用户提供的角色设定图 |

## 附录 B：ANIBUZZ 参考图 vs 冲浪阅读适配差异

| ANIBUZZ 原始元素 | 冲浪阅读适配 | 原因 |
|-----------------|-------------|------|
| 社交 Feed 流 | 阅读内容流 | 产品形态不同 |
| 用户头像/社交身份 | 阅读进度/书架 | 功能不同 |
| 心形互动（玫瑰色） | 收藏功能保留玫瑰色 | 唯一共用场景 |
| 多角色（不同用户不同形象） | 单一小鲸（品牌固定） | 产品不需要 UGC 角色 |
| 明亮蓝 CTA 按钮 | whale-blue CTA（色调更深沉） | 匹配海洋沉浸主题 |
| 浅色文字 `#e8f0f4` | 保持（已匹配） | 暗色背景上的近白文字 |
| 暗色背景 `#162834` | 改用 `#142836`（更深） | 更强沉浸感 |

---

> **文档结束**
>
> 下一阶段：根据此规范，进入 **Phase 1 工程实施**（主题色板 + Glass Card 组件 + 全局视觉基调替换）。
> 或先进入 **美术资产生产**（用 AI 工具批量生成上述资产清单中的 P0 资产）。
>
> *由 App 视觉总监输出 · v2.0 Draft A · 基于小鲸角色设定表 + ANIBUZZ 视觉参考*

# 冲浪阅读 · M2.5 增量方案（PRD + 架构设计 + 任务拆解）

> 版本：v0.2.0 → v0.2.5（规划中）
> 范围：**仅 Tier 1 三项**，纯前端 UI/UX 打磨
> 视觉风格：阅读页=纸质拟物（暖米纸+中文衬线）；App 外壳=Material You（海洋青 `0xFF0E7490` + 圆角卡片）
> 红线：**不碰 `data/` 数据层、不引入 Room、不碰后端、不增加重型依赖**；不与 M3（书架/离线）重叠

---

## 一、产品目标（产品经理 · 许清楚）

在不改动任何数据链路的前提下，把「能用的阅读壳子」升级为「愿意停留的阅读体验」：让阅读菜单有质感、页面切换有过渡、设置面板有秩序，整体透出纸质书的高级感。

---

## 二、用户故事

1. 作为读者，我希望点开阅读菜单时看到的是带图标的圆角卡片，而不是一排文字按钮，这样更有高级感。
2. 作为读者，我希望从书城点进书、再进阅读时页面是平滑过渡的，而不是硬切，减少视觉跳跃。
3. 作为读者，我希望在设置里调字号时能立刻看到一段样张文字随之变化，而不是盲调。
4. 作为读者，我希望设置面板的选项是有分组、有图标的，而不是一长串控件堆在一起。

---

## 三、需求池（P0 = 本次必做，P1 = 顺带优化）

| # | 需求 | 优先级 | 功能/交互细节 | 验收标准 |
|---|------|--------|--------------|----------|
| R1 | 阅读菜单层重做 | P0 | 顶栏/底栏改为带圆角+轻微 elevation 的浮动卡片（非全宽贴边）；按钮改用图标（返回/目录/上一章/设置/下一章）；用 `AnimatedVisibility` 做淡入+下滑（顶）/上滑（底）进出动画，替代当前 `if(menuVisible)` 硬显隐 | 菜单弹出/收起有动画；按钮为图标而非纯文字；卡片有圆角与阴影 |
| R2 | 全应用页面转场 | P0 | `AppRoot.kt` 各 `composable` 加 `enter/exit/popEnter/popExit` 转场，使用内置 `slideInHorizontally + fadeIn`（**不引入 Accompanist**） | 书城→详情→阅读三处跳转均有横向滑动+淡入过渡，无硬切 |
| R3 | 设置面板分区+图标+预览 | P0 | `ReaderSettingsSheet.kt` 用 `HorizontalDivider`+小标题分组；每行配图标；字号区上方加「实时预览样张」一段文字，随字号/主题即时变化 | 面板有清晰分区与图标；拖动字号时样张同步变化 |
| R4 | 阅读正文衬线化（纸质感） | P1 | 阅读正文 `FontFamily` 由 `Default` 改为 `Serif`（系统衬线，零字体文件）；`Theme.kt` 补 `shapes`（圆角）让外壳卡片/按钮更圆润 | 阅读正文呈衬线体；App 外壳控件为圆角 Material You 风格 |
| R5 | 菜单栏组件化 | P1 | 将菜单顶/底栏抽成 `ui/reader/components/ReaderMenuBar.kt`，降低 `ReaderScreen.kt` 体积、便于复用 | `ReaderScreen.kt` 菜单相关代码收敛到独立组件 |

> 明确**不做**（防越界）：仿真翻页、阅读页进度条（属 Tier2）、字体文件打包/字体选择（Tier3）、详情页/书城美化（Tier2/3）、书架/离线/进度记忆（M3）、任何后端改动。

---

## 四、UI 设计稿描述（文字 + 布局示意）

### 4.1 阅读菜单层（R1）
```
        ┌─────────────────────────────────┐   ← 顶栏卡片：圆角16dp + elevation，左右留 12dp 边距
        │ ←  书名（单行省略）      3/120  │
        └─────────────────────────────────┘
                                            （正文区，点击左1/3上一页·中1/3菜单·右1/3下一页）

        ┌─────────────────────────────────┐   ← 底栏卡片：同样圆角浮动
        │  📑目录    ‹上一章   ⚙设置  下一章› │   图标按钮，禁用态半透明
        └─────────────────────────────────┘
```
- 进出动画：顶栏 `fadeIn + slideInVertically(-height)`；底栏 `fadeIn + slideInVertically(+height)`；收起反向。

### 4.2 页面转场（R2）
- 书城 → 详情：新页从右侧滑入 + 淡入，旧页向左滑出。
- 详情 → 阅读 / 返回：同样的横轴共享过渡（`pop*` 反向）。
- 时长约 250–300ms，缓动用内置 `FastOutSlowInEasing`。

### 4.3 设置面板（R3）
```
┌─ 阅读主题 ────────────────────────┐
│  (米纸)(护眼绿)(夜间)(纯黑)  ◯选中描边 │
│                                    │
│  「预览样张」：随字号/主题实时变化的一段  │
│  文学示例文字（衬线、当前主题配色）      │
├────────────────────────────────────┤  ← HorizontalDivider
│ 🔤 字号        A-  18  A+           │
│ 📏 行距        ┈┈●┈┈  1.7           │
│ 🔲 段距   [紧凑][适中][宽松]          │
│ 📐 边距   [窄][标准][宽]             │
├────────────────────────────────────┤
│ 📖 翻页   [覆盖][滚动]              │
│ ↪  首行缩进            ┌──┐         │  ← Switch
│ 🔊 音量键翻页          ┌──┐         │
│ 💡 屏幕常亮            ┌──┐         │
└────────────────────────────────────┘
```

---

## 五、增量架构设计（架构师 · 高见远）

### 5.1 改动文件清单（相对 `app/src/main/java/com/xiaoswz/reader/`）

| 文件 | 改动类型 | 说明 |
|------|----------|------|
| `ui/theme/Theme.kt` | 修改 | 补 `shapes`（圆角）；新增 `ReaderBodyFont = FontFamily.Serif` 等共享常量 |
| `ui/AppRoot.kt` | 修改 | NavHost 各 `composable` 加 enter/exit/pop* 转场 |
| `ui/reader/ReaderScreen.kt` | 修改 | 菜单层改圆角卡片 + 图标 + `AnimatedVisibility` |
| `ui/reader/ReaderSettingsSheet.kt` | 修改 | 分区 + 图标 + 字号预览样张 |
| `ui/reader/components/ReaderMenuBar.kt` | 新增（R5） | 封装菜单顶/底栏，供 `ReaderScreen` 调用 |

### 5.2 关键实现要点（零新依赖论证）

- **R1 动画**：`AnimatedVisibility` / `slideInVertically` / `fadeIn` 来自 `androidx.compose.animation`（Compose BOM 内置），无需新增依赖。菜单卡片用 `Surface(shape=RoundedCornerShape(16.dp), color=OverlayBg, tonalElevation=3.dp)` 实现圆角浮动；图标来自已依赖的 `compose-material-icons-extended`（`Menu`/`ArrowBack`/`ArrowForward`/`Settings`/`NavigateBefore`/`NavigateNext` 等）。
- **R2 转场**：`slideInHorizontally` / `slideOutHorizontally` / `fadeIn` / `fadeOut` 同属 `androidx.compose.animation`，navigation-compose 2.8.5 的 `composable()` 原生支持 `enterTransition/exitTransition/popEnterTransition/popExitTransition` 参数。**刻意不用** `materialSharedAxisX`（属 Accompanist，违反零重型依赖红线）。
- **R3 分区**：`HorizontalDivider` 在 Material3 1.3.x（`androidx.compose.material3.HorizontalDivider`）已提供；图标同上。预览样张直接复用传入的 `settings.fontSize` 与 `ReaderThemes[themeIndex]` 的 background/text 色，天然实时响应。
- **R4 衬线**：阅读正文 `Text` 的 `fontFamily = FontFamily.Serif`（系统衬线，无需 `res/font` 文件）。`Theme.kt` 补 `shapes` 让外壳 `Card`/`Button` 默认圆角，得到 Material You 观感。
- **R5 组件化**：`ReaderMenuBar.kt` 导出 `ReaderTopBar(...)` / `ReaderBottomBar(...)` 两个 `@Composable`，`ReaderScreen` 内用 `AnimatedVisibility` 包这两个组件即可。

### 5.3 数据流 / 调用关系
- 不涉及 `data/` 与后端；所有改动在 UI 层内部。
- `ReaderSettingsSheet` 仍接收 `state.settings` 与 `onChange`，预览样张为纯展示态，不引入新持久化字段。
- `AppRoot` 转场为声明式修饰，不影响各 Screen 已有业务逻辑。

### 5.4 任务列表（有序 + 依赖）
1. **T1** `Theme.kt`：补 `shapes` + 导出 `ReaderBodyFont` / 主题色常量 — *无依赖，先行*
2. **T2** `AppRoot.kt`：加页面转场 — *独立*
3. **T3** `ReaderSettingsSheet.kt`：分区 + 图标 + 预览样张（可选依赖 T1 的字体常量）— *依赖 T1（弱）*
4. **T4** `ReaderMenuBar.kt`（新增）+ `ReaderScreen.kt` 菜单层改造 — *独立（依赖 compose-animation 内置）*
5. **T5** 联调与真机手感验证 — *依赖 T1–T4*

### 5.5 依赖包
- **新增依赖：无。** 全部使用既有 Compose BOM / Material3 / material-icons-extended / navigation-compose 已提供的 API。

### 5.6 共享约定
- 菜单卡片统一圆角 `16.dp`、elevation `3.dp`、左右外边距 `12.dp`（浮动不贴边）。
- 转场统一 300ms + `FastOutSlowInEasing`。
- 图标按钮统一 `IconButton` + `Icon(tint=OverlayText)`，禁用态 `OverlayTextDim`。

---

## 六、待确认问题（需你拍板）

1. **转场风格**：横轴滑动（推荐，更贴合阅读 App）还是纯淡入？
2. **纸质衬线**：M2.5 先用系统 `FontFamily.Serif`（零成本），还是现在就打包思源宋体 OFL（属 Tier3 字体选择，会引入字体文件/略增包体）？
3. **设置预览样张文案**：用固定文学示例（如「月落乌啼霜满天……」）还是动态取本书首段？
4. **菜单底栏**：五个入口（目录/上一章/设置/下一章 + 顶部返回）布局是否 OK？是否需要把「设置」单独放顶部？
5. **R5 组件化**：是否接受新增 `ReaderMenuBar.kt`（更整洁，但多一个文件）？

---

## 七、验收标准（M2.5 完成定义）

- [ ] 阅读菜单为圆角浮动卡片 + 图标按钮，进出有动画
- [ ] 书城/详情/阅读三处跳转有横向滑动+淡入过渡
- [ ] 设置面板有分区、图标、字号实时预览样张
- [ ] 阅读正文为衬线体，App 外壳控件为圆角 Material You 风格
- [ ] `assembleDebug` 通过；包体仍 < 10MB（无新依赖）
- [ ] 不触碰 `data/`、后端、Room；M3 接口不受影响

---

## 八、实施记录（M2.5 已完成 ✅）

### 8.1 五点待确认 — 已拍板（用户授权主理人直接决策）
1. **转场风格**：横轴滑动 + 淡入（300ms，`FastOutSlowInEasing`），未引入 Accompanist。
2. **纸质衬线**：使用系统 `FontFamily.Serif`，零字体文件、不增包体（打包思源宋体留待 Tier3）。
3. **预览样张文案**：固定文学示例（朱自清《荷塘月色》片段），随字号/主题实时变化。
4. **菜单底栏布局**：维持 目录 / 上一章 / 设置 / 下一章 + 顶栏返回。
5. **R5 组件化**：已新增 `ui/reader/components/ReaderMenuBar.kt`。

### 8.2 实际改动文件
| 文件 | 改动 |
|------|------|
| `ui/theme/Theme.kt` | 补 `shapes` 圆角；导出 `ReaderBodyFont=FontFamily.Serif`；暖化米纸色 `#F3EAD8` |
| `ui/AppRoot.kt` | NavHost 三页面加 `slideIn/OutHorizontally + fadeIn/Out` 转场 |
| `ui/reader/components/ReaderMenuBar.kt` | **新增**：顶/底栏圆角浮动卡片 + 图标按钮 |
| `ui/reader/ReaderScreen.kt` | 菜单层 `AnimatedVisibility` 进出动画 + 调用 `ReaderMenuBar`；清理无用 `Overlay*` 常量 |
| `ui/reader/ReaderSettingsSheet.kt` | `HorizontalDivider` 分区 + 每项图标 + 字号实时预览样张 |
| `app/build.gradle.kts` | `versionCode` 3→4，`versionName` 0.2.1→0.2.5 |

> 依赖零新增（图标库 `material-icons-extended` 本就在版本目录 `androidx-compose-material-icons` 中）。

### 8.3 构建与分发
- 构建：`JBR 17`（`C:\Program Files\Android\Android Studio\jbr`）+ `./gradlew assembleDebug`，`BUILD SUCCESSFUL`。
- 产物：`lan-update/surf-reader-0.2.5.apk`（debug 签名，约 18.4MB，仅分发用）。
- 局域网推送：项目自带 `UpdateManager`/`UpdateDialog`（默认服务器 `http://192.168.2.4:8765`），PC 局域网 IP 正是 `192.168.2.4`。`lan-update/` 下已放置 `version.json`(code 4) + APK + `lan_update_server.py`（封装 `http.server`，绑定 `0.0.0.0:8765`）。手机 App 启动即自动检测 M2.5，点「下载→安装」即可 OTA。

### 8.4 手机端更新步骤
1. 手机与 PC 连同一 Wi-Fi（同一局域网）。
2. 打开冲浪阅读 → 书城页顶部「检查更新」图标（或启动自动弹窗）→ 看到「发现新版本 v0.2.5」→ 点「下载」。
3. 首次安装未知来源 APK，系统会跳到「设置」要求授权「允许安装未知应用」，授权后回到 App 再点「安装」。
4. 若手机尚未装过本 App：用手机浏览器打开 `http://192.168.2.4:8765/surf-reader-0.2.5.apk` 直接下载安装即可。

### 8.5 风险/备注
- 分发用 **debug 签名** APK；正式上架需补 `release` 签名（`buildTypes.release` 当前无 `signingConfig`）。
- 仿真翻页、阅读页进度条、字体文件打包等属后续 Tier/M3，本次未做，与 M3 不冲突。
- 保持红线：未碰 `data/`、后端、`Room`；M3 接口不受影响。
